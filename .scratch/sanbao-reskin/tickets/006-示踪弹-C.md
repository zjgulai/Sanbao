# 006 示踪弹 C：启动屏端到端 + spin 动画保留证明

**类型**: 示踪弹
**Blocked by**: 003

## 目标
换掉开机字标，同时**用断言证明加载动画还活着**——动画的弧色取自 `--dsw-alias-brand-primary`，本张要确认这条自动跟皮的路径没被改断。

## 涉及层
- [x] S-A：`stacked` 字标由当前名源生成（Inter SemiBold 转路径，无运行时字体依赖；不提前更改名源值）
- [x] S-C：`brand-payload-wordmark.txt` 换新载荷；可执行构造锚解析，删除独立 ROOT 文本字标段
- [x] 判据层：ADR-0136 D6（2026-09-20 用户修订）：spin、`animation-duration=2s`、conic 弧色解析值 == 源 accent；仅通过 bundle 重放改周期
- [x] 装机层：用户授权后改写启动 JS/CSS，备份并重签，Info.plist 未变
- [x] 测试：错误动画名、时长、颜色、渐变类型、零角度反例，以及写入失败回滚
- [ ] 收口：整体门禁仍有并发侧栏/ADR 与既有 profile 红项，未提交、未推送；真机启动画面的用户目视确认尚未取得

## 验收标准
- 真机开机：新字标呈现，且圆环仍在转
- 三条动画断言全绿，且反例测试能红
- `patch-anchors`（full 模式）绿；`brand-replay-selftest` R1–R6 绿
- 字体授权文件 `Inter-OFL-1.1.txt` 随产物入库

## 验收回执（2026-09-20，未最终收口）

- 新周期由用户从 0.8s 修订为 **2s**。实际 bundle 的 CSS diff 已核对只有 `.8s → 2s`；keyframes、进度弧、主题颜色引用逐字节未变。字标使用当前名源 `LUTE Agentic System`，不提前迁移应用名称。
- `node --test scripts/gates/boot-animation.test.mjs scripts/gates/brand-wordmark.test.mjs scripts/gates/brand-derivatives-sync.test.mjs scripts/gates/app-icon-build.test.mjs scripts/gates/brand-avatars-pin.test.mjs`：22/22 pass；`brand-replay-test.sh`：8/8 pass。
- 浏览器执行实际安装资源提取的 BootPage，与真实 CSS/主题供给组合：亮暗两态 `duration=2s`、持续旋转、arcColor==accent；进度弧 72°→180°→288°。五类突变均判红；[浏览器读数](006-browser/readings.json)、[亮色截图](006-browser/boot-light.png)、[暗色截图](006-browser/boot-dark.png)来自隔离浏览器预览，不冒充原生窗口截图。
- 重放器在字标与 CSS 都预检通过后写入；拒绝注释/字符串伪锚、多锚、重复样式、data 标记覆盖。模拟第二文件写入失败时恢复两文件原字节并清理临时文件。
- 字体与 OFL 采自固定供体提交，已入本票工作树并登记 pin；许可也进入 assemble 随包文件集。
- 用户授权正常退出后装机：备份 `~/Library/Application Support/LUTE/brand-backup/20260920-015428/`。仅启动 JS/CSS 两文件发生变化，Info.plist SHA-256 未变。严格 codesign exit 0，重放 `BRAND ALL VERIFIED`；`DSH_APP=... bash packaging/verify-patches-v2.sh` 返回 `PATCHES v2 ALL VERIFIED`（定向运行，非完整 gate:full）。
- [本次启动证据](006-browser/startup.json)：runId `c434f106-70b8-4316-859f-7eca70400f9a`，renderer 启动/完成各一次、零失败事件，36728ms 到 health-commit/healthy；新增日志 345 字节，零看门狗告警，另有 LAN HTTPS 私钥不可读取告警（未改凭证）。
- 首次等待启动完成误用文件偏移，因生命周期文件被新 run 覆盖而报告超时；回读完整文件以新 runId/时间戳确认健康，撤回该误报。utility CPU 曾单点 99.2%，随后复测 0.9%，renderer 0%；不以单点低 CPU 否认该峰值。
- [最终 quick 门禁](006-gate-quick.json)：98/109 pass、3 skip、8 fail。006 的两项启动自测、名源一致性、图标重放和 pin 均 pass；失败涉及其他会话侧栏包身份/锁文件/目录/行轴、ADR-0140/0141 账本与索引，以及既有 profile 装载点漂移。完整 gate:full、本票最终提交/推送尚未完成，不放宽判据或替他人覆盖文件。
- 双轴审查发现项已通过反例修复，见[决策 Note](../../../docs/notes/implemented/surface/2026-09-20-boot-wordmark-and-spin.md)。当前只声明功能实现与授权装机通过；原生启动画面尚待用户目视确认。

## 备注
用户明确要求保留启动动画，并将旋转周期调整为 2s。

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
