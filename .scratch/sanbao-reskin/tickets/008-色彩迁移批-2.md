# 008 色彩迁移批 2：`dsh-root-brand-local`

**类型**: 宽重构·迁移
**Blocked by**: 007

## 目标
把品牌座包内 8 处主色字面量改为读源，含 `RootMark`/`HeroRootBrand` 的描边与回退值。

## 涉及层
- [ ] 源：`brand.tsx` 的 `BRAND_GREEN`/`BAR_GRAY` 常量与 6 处 `var(…, #58b848)` 回退
- [ ] 派生：`lib/client.js` 走 `shared-sync` 重生成
- [ ] 测试：`brand.test.tsx:12-13` 改读源；`brand.test.tsx` 的文案断言留到 015（本张不碰名字）
- [ ] 装机：`theme-tokens-live.mjs` 解析值复核

## 验收标准
- 包内不再出现 `#58b848` 字面量（grep 为空）
- `pnpm run gate` 绿；真机侧栏品牌座与空会话 hero 颜色正确
- 本张 diff 内**不含任何品牌名变化**（色彩与名字分片，出问题可各自回滚）

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
