# 016 名字迁移批 2：`<title>` + Info.plist + 4 个 Helper 名

**类型**: 宽重构·迁移
**Blocked by**: 003, 015

## 目标
走既有 S-C 声明表换掉窗口标题、显示名与辅助进程名——**不新建资产写入通道**。

## 涉及层
- [ ] S-C：`brand-replay.sh:44-53` 文件清单、`:147-155` 标题替换、`:161-180` Helper 名、`:190-201` Info.plist
- [ ] 寻址面：**不动** bundle 路径、可执行文件名、`appId`、`productName`
- [ ] 判据层：`brand-replay-selftest` R1–R6 覆盖新声明
- [ ] 装机：`codesign --verify --deep --strict` 通过

## 验收标准
- 真机：Dock 悬停名、应用菜单名、窗口标题均为 `Sanbao`
- `/Applications/DSH Desktop.app` 与 `Contents/MacOS/DSH Desktop` **未改名**（ADR-0131 D2）
- `brand-replay-selftest` 绿；`--check` 模式在无漂移时全 OK

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
