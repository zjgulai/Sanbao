# 017 名字迁移批 3：recovery / setup-wizard / desktop-dialog 走 bundle 重放

**类型**: 宽重构·迁移
**Blocked by**: 003, 016

## 目标
让三处住在 gitignored 嵌套仓里的品牌文案**进入 git 可审面**——在 S-C 加声明，不改 vendor 源码。

## 涉及层
- [ ] S-C：为三份 HTML/JS 新增重放声明（现状 `brand-replay.sh` 完全覆盖不到）
- [ ] 边界：`vendor/dsh-desktop/` 源码**一行不改**（只读 pin，ADR-0008）
- [ ] 判据层：`brand-replay-selftest` 覆盖新声明；`resource-path-reachability` 验路径可达
- [ ] 装机：真机进入恢复模式看到新文案

## 验收标准
- 三条品牌改动在 `git diff` 里可见、可 revert
- `vendor/dsh-desktop/` 的 git 状态未被本工单触碰（有证据）
- 真机：恢复模式、安装向导、桌面对话框三处文案已是新产品名

## 备注
现状是「在 git review 看不见的地方改出货文案」，这条路径本轮被封死。

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
