# 028 打包链：签名身份 + 旧证书保留裁决

**类型**: 宽重构·迁移
**Blocked by**: 026, 027

## 目标
换签名身份，并把「老 bundle 能否复现构建」这个悬而未决的问题当场裁决。

## 涉及层
- [ ] 打包层：`assemble.sh:355`、`build-setup-app.sh:49`、`ensure-signing-identity.sh:16` 默认身份
- [ ] S-A：身份串取自名源 `signingIdentity`，不写字面量
- [ ] 裁决层：旧身份证书是否保留 → 决定老 bundle 能否复现构建，结论写进 Note
- [ ] 判据层：两处 `security find-identity` 前置断言通过

## 验收标准
- `security find-identity -v -p codesigning` 能列出 `Sanbao Code Signing`
- 完整构建一次 DMG，`codesign --verify --deep --strict` 对主 app 与 Setup.app 均绿
- 旧证书保留与否有书面结论，且结论可复现（换机重建路径验证过）

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
