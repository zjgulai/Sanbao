# 009 色彩迁移批 3：`packages/surfaces/*`

**类型**: 宽重构·迁移
**Blocked by**: 007

## 目标
迁移 surfaces 组的色彩字面量：newapp Brand block、skill-panel 六处回退、capability-hub、algo-skills 的 `layer-icons`。

## 涉及层
- [ ] 源：`newapp.module.css` Brand block、`skill-panel.module.css` ×6
- [ ] 生成：`gen-layer-icons.mjs` 两份（`algo-skills` 与 `overseas-skills`）描边色改源值
- [ ] 跨包不变量：`layer-icons` 两包**字节相同**必须维持
- [ ] 测试：`layer-icons.spec.ts` 改读源；`overseas-skills/test/layer-icons.spec.mjs` 的字节相同断言
- [ ] 判据层：`verify-layer-icons.mjs:91` 改读源；`sidebar-row-axis` 的 fixture 断言 `--lute-brand` 不出现在轴规则体内——本张不得违反

## 验收标准
- surfaces 组内 `#58b848` 字面量归零
- 两包 `layer-icons` 仍逐字节相同（跨包不变量未破）
- `sidebar-row-axis` 绿；`pnpm run gate` 绿

## 追溯
- 规格：`docs/specs/2026-09-19-sanbao-brand-reskin.md`
- 射程：`.scratch/sanbao-reskin/surface-map.md`
- 计划：`.scratch/sanbao-reskin/plan.md`
