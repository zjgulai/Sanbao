# 架构决策记录（ADR）

## 索引

| 编号 | 标题 | 状态 | 决策记录 |
| --- | --- | --- | --- |
| ADR-0001 | GitHub monorepo 管理：单仓库发布 DSH 二开平台 | accepted（2026-09-06） | — |
| ADR-0002 | DMG 发布：GitHub Releases 附件 + release/ 哈希清单 | accepted（2026-09-06） | — |
| ADR-0003 | 版本策略：单平台版本 vX.Y.Z + 插件 package.json 对齐 | accepted（2026-09-06） | — |
| ADR-0004 | 仓库收录范围：全平台 monorepo（含 81-Skills 公开） | accepted（2026-09-06） | — |
| ADR-0005 | rc.1 基座迁移立项（LUTE 2.0.0，DSH 基线 2.0.4→2.0.5） | accepted（2026-09-10） | — |
| ADR-0006 | 上游版本跟进策略（月度观察窗 + 红线触发制） | accepted（2026-09-10） | — |
| ADR-0007 | 二开平台重构级别：三期推进（骨架 → 结构收敛 → 契约与清账） | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0008 | harness submodule 初始化但仅作只读参照系 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0009 | 文档消费者与语言策略：主脊柱中文单语 + 客户链独立用户向文档 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0010 | 包平面三分治理：自研层 / npm 外部层 / 处置候选层 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0011 | 能力五组归位 + 生成式目录墙（不引入 pnpm workspace） | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0012 | 包名不改、目录名归一、新增身份三元组门禁 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0013 | 资产分级处置：删除 / 归档出工作树 / 纳入版本管理 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0014 | 门禁全量硬门槛 + 只减不增的临时豁免（N5=A2） | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0015 | 决策记录双轨分职：ADR 时间线 + 本地 Notes（强制留痕） | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0016 | 嵌套仓库治理：受管目录不得含未声明的独立仓库 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0017 | 包的类型检查必须指向内建运行时的类型，而非应用内打包产物 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0018 | 构建产物 lib/types 的入库边界与 build 可执行性 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-lute-refactor-three-phase.md) |
| ADR-0019 | 对官方 UI 的改写锚必须运行时解析，禁止把 CSS-module 哈希写进产品代码 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-root-brand-live-resolver.md) |
| ADR-0020 | 岗位小队 = 单 preset 的人格 + 技能并集，不做多 preset 挂载 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-role-squad-contract.md) |
| ADR-0021 | Preset 承载材料声明时，必须显式披露「声明 vs 供给」的差 | accepted（2026-09-11） | [Note](../notes/implemented/architecture/2026-09-11-preset-supply-disclosure.md) |
| ADR-0022 | 岗位头像走官方 `icon` 字段，图标库是唯一事实之家 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-preset-avatar-contract.md) |
| ADR-0023 | 删除被会话引用的 preset：默认值先迁移、归档先验字节、损失需显式接受 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-preset-cleanup.md) |
| ADR-0024 | 上下文压缩加固：容量表实测 + 摘要路由解耦 + 确定性压缩按需切换 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-context-compaction-hardening.md) |
| ADR-0025 | `webServer.register` 一次只收一条路由；传数组会静默失效 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-role-matrix-route-registration.md) |
| ADR-0026 | 对官方 UI 的样式覆盖必须与注入顺序无关（!important + 样式标签居末） | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-root-brand-hero-override-cascade.md) |
| ADR-0027 | 模态浮层进浏览器 top layer（`<dialog>` + `showModal()`），不参与 z-index 竞争 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-role-matrix-top-layer-drawer.md) |
| ADR-0028 | 启动器只拥有「连接」，不拥有事实；侧边栏启动位与官方键各占一半 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-newapp-launcher-join-not-facts.md) |
| ADR-0029 | 主题 Token 的判据要两侧共同证伪：真实 CSS 引擎 + 真实供给函数 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-theme-token-verdicts-two-sided.md) |
| ADR-0030 | 在 `/api` 下注册 exact 路由的插件绕开平台自己的来源栅栏——必须逐个插件打栅栏 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-worktable-trust-fence.md) |
| ADR-0031 | 技能分类必须与岗位矩阵同构；缺口认领必须与分类落点自洽 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-preset-skills.md) |
| ADR-0032 | 跨框架注入的共享核心：「未挂载」只有一种表示，且其契约必须在真框架环境证伪 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-sidebar-entry-null-pane.md) |
| ADR-0033 | Native Agent 产品 = 项目目录里的声明 + 实现包；启动器只发现与开入口 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-native-agent-product-form.md) |
| ADR-0034 | 功能的提示词骨架 = 多技能组合（岗位方法 + 产品契约）；功能内部下沉为 `steps[]` | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-native-agent-product-multiskill-skeleton.md) |
| ADR-0035 | 技能效果的断言必须有实测背书；改动须过 held-out 验证门，未过门一律回滚 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-effect-eval.md) |
| ADR-0036 | 产品包由岗位 preset 的行挂载，且故意不声明 `dsh.bundle`——把「局部技能」变成结构上不可搞错的事 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-product-package-preset-mount.md) |
| ADR-0037 | 产品包挂两层，靠 `role` 分离；局部技能的机关从「缺声明」换成「显式关断」 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-product-package-two-layer-mount.md) |
| ADR-0038 | 共享来源栅栏的判据必须是「拒绝」而不是「抛异常」；可选服务查询一律尽力而为 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-shared-fence-deny-not-throw.md) |
| ADR-0039 | 语义 Token 三条法则（只引用官方已声明名 / 兜底=实测浅色值 / 品牌色非主题色）；门禁的「引用」只认 `var()` | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-theme-token-three-laws.md) |
| ADR-0040 | 开发脚本起的子进程必须是真 node（`process.execPath` 在 pnpm 下是宿主 Electron，且「退出码 0 却没有输出」）；判据先剥注释与字符串 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-node-interpreter-not-execpath.md) |
| ADR-0041 | 已安装的技能本体就是分类的运行时家；归位与接线必须分开显示 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-algo-skills-surface.md) |
| ADR-0042 | 外部插件注册 `settings.section` 必须走 `slots.inject`；直接 `register` 会静默丢行 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-settings-slot-must-inject.md) |
| ADR-0043 | 门禁报失败必须给出为它负责的读数：输出按流分流保留，退出码不得折算 | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-script-evidence-by-stream.md) |
| ADR-0044 | 归位判定必须自带逐字证据、落在自己的家（不改接线、不写技能 frontmatter）；四层下钻里归位与接线分开显示 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-overseas-skills-role-tree.md) |
| ADR-0045 | 产品矩阵只列已产品化的产品、一张大卡一个产品；工作台与控制室的 UI 全部卸载（部分取代 ADR-0028 与 ADR-0033 第 6 条） | accepted（2026-09-12） | [Note](../notes/implemented/simplification/2026-09-12-newapp-product-matrix-ablation.md) |
| ADR-0046 | 可选服务只能用 `ctx.get` 读（裸读会抛，`?? 兜底` 是假兜底）；Remote 三元组必须按位传参并读回 `ok:false` | accepted（2026-09-12） | [Note](../notes/implemented/contract/2026-09-12-newapp-card-open-context.md) |
| ADR-0047 | p2s 语料的三行级缺陷改在流水线里修（不手改 1338 个文件）；出处按六档如实渲染，不做无差别还原 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-corpus-rebuild.md) |
| ADR-0048 | p2s 卡 ⑦ 段是 60 行上限的预览节选，卡面按实测口径渲染而非转发源站自述；卡页「路径」只作转述（部分取代 ADR-0047 的代价与边界两条） | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-code-availability.md) |
| ADR-0049 | 完整实现从未丢失：读语料 vault 的 git 明文恢复（1,302/1,338），落 `references/implementation.py`；三档分层且无 oracle 的一档必须标注未核对（订正 ADR-0048 的两条全称断言） | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-source-code-recovery.md) |
| ADR-0050 | 代码围栏按「宽松枚举候选 + 语法定终点」抽取，不按 Markdown 配对（确证 1,262→1,277、未恢复 36→21；订正 ADR-0049 的「那 21 张是真实的代际差异」） | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-fence-candidates.md) |
| ADR-0051 | `parses=False` 拆成「源码写坏 5」与「判据未定终点 15」两类，逐卡带 `defect`/`defect_line`/`defect_detail`（订正 ADR-0049 收尾那句「20 张都因全角标点写坏」） | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-paper2skills-parse-defects.md) |
| ADR-0052 | 第三方技能入库**不保留 `.git`**（改由锚定 commit SHA + 上游 tarball 承担可复现与回滚）；入库必须**挂岗或显式归为通用型**，不允许既无岗位也无通用分型的裸条目 | accepted（2026-09-12） | [Note](../notes/implemented/capability/2026-09-12-third-party-skill-intake.md) |
| ADR-0053 | 岗位能力事实读它的家（preset manifest + 技能本体），**不派生第二份清单**；宿主路由现读现投影，挂载锚零哈希 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-composer-role-capabilities.md) |
| ADR-0054 | 改动生效的判据是**装载点的字节**（`profile/node_modules`，不是 `vendor/`）：门禁从「副本有没有这个文件」升到「字节一不一样」，且只钉可执行产物面 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-loadpoint-bytes-drift.md) |
| ADR-0055 | 依赖层可复现的判据：清单与锁文件逐条 specifier 相等、依赖不得指向机器绝对路径、同名不得跨字段重复声明，离线判定（`deps-reproducible`）；产物未入库的包 `build` 先于 `test` | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-dependency-reproducibility.md) |
| ADR-0056 | 出货的 preset **不烘焙任何外部产品行**（`PRODUCT_MOUNTS` 置空，本机产品走本机装配）；机器路径两个前缀都参数化（`__DSH_HOME__` / `__LUTE_PROJECT_ROOT__`）；出货树机器路径**只减不增**（签名前机读守卫） | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-shipping-surface.md) |
| ADR-0057 | 发布产物的发布语义：`release/<版本>/` 要么不存在、要么是**完整且已全部通过终验**的集合；构建在临时区完成、**原子改名就位**；同号重制默认拒绝，`--force` 时旧产物**归档不删除**；构建锁记 pid（无主可回收、活锁仍拦截）；发布语义由沙箱化断言守护 | accepted（2026-09-12） | [Note](../notes/implemented/architecture/2026-09-12-release-atomic-publish.md) |
| ADR-0058 | 发布清单入库：仓库根 `release/<version>.sha256` 是**清单的家**（进 git），由 `sign-and-dmg.sh` 在产物**原子就位之后**生成，不再依赖 SOP 里的人工步骤；清单必须带源凭据（`source_commit` / `source_dirty` / `profile_snapshot`，由 `assemble.sh` 在**装配时刻**记录）；时序固定为**构建 → 提交清单 → 打 tag**，tag 才担保得住字节；失败方向是「产物完好、清单缺失」 | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-release-manifest.md) |
| ADR-0059 | 中转站接入以**逐模型实测工具调用**（`tool_choice: "auto"`）为准入判据——schema 是否声明 `tools` 不可作依据（实测二者无关且方向相反：全部 Claude Opus/Sonnet 系接受 `tools` 却不产出 `tool_calls`）；PoYo 拆 `poyo`（11 个实测稳定）/ `poyo-responses`（3 个）两条路由共用一份凭据；pi-ai 的 `detectCompat` 对未特判站点落默认值，5 处与站方 schema 不一致必须显式纠偏；因上游稳定性未达标不写进任何默认位 | accepted（2026-09-13，同日修订） | [Note](../notes/implemented/capability/2026-09-13-poyo-relay-route-intake.md) |
| ADR-0060 | 模型准入判据补两关：**tool_call 必须回灌验证**（缺 `type:"function"` 只在客户端回传路径上暴露，单轮测不出）+ **多轮重复**（实测 33% 间歇静默失败）；判据由三关升为四关；抽测只作并列决胜、不得据此宣称模型更强；筛选目标是「每族一条当代最好的」而非「不漏掉任何可用项」——硅基流动清单据此 25 → 9 | accepted（2026-09-13） | [Note](../notes/implemented/capability/2026-09-13-siliconflow-model-audit.md) |
| ADR-0061 | 新应用抽屉同时兼容 `ctx.agentPresets` 与 `connection.api.agentPresets` 两种 preset 选择器载体；深链星探 KOL-Hunter 走本机 profile 本地装配，不再烘焙进出货 preset；新增 DMG 打包发布 SOP | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-newapp-agentpresets-channel-and-kol-hunter-local.md) |
| ADR-0062 | 新应用抽屉增加「业务系统」第二分区：外链只由宿主打开且只接受 slug；本包从此拥有一份 catalog（部分修订 ADR-0045） | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-newapp-systems-section.md) |
| ADR-0063 | 出货 app 改用固定身份的证书签名：adhoc 的指定要求字面上就是 CDHash，TCC 授权因此随字节失效——「重新授权一次」实为**每版一次**；改建自签身份后换签只付一次；签名身份升为「签不出来即失败」的受门禁保护构建输入（修订 PLAN 决策 D2） | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-signing-identity-tcc-stability.md) |
| ADR-0064 | shell 变量名边界纳入门禁：`$VAR` 紧跟多字节字符（如全角括号）会被 bash 并入变量名，`set -u` 下直接中断——实测让 2.3.0 首次装配在 §5 作废，且 4 处现场中有一处恰在**错误报告路径**上；修复统一改 `${VAR}`，新增 `shell-var-multibyte` 硬校验（注释与位置参数不报） | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-shell-var-multibyte-guard.md) |
| ADR-0065 | 生产模式禁用 HMR 热更：运行中替换 app bundle → 宿主推 rebuilt 帧 → 生产 renderer 无 dev:web runtime 热更崩溃 → 整屏白屏（2026-09-13 实测，Cmd+R 可恢复）；机制修复为 host 侧生产守卫（`process.defaultApp`/`DSH_DEV` 判定），流程修复为安装器替换前强制退出运行实例，并恢复 `console-message` 转发消除静默 | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-hmr-production-guard.md) |
| ADR-0066 | 安装向导必须自己定位载荷：`Setup.app` 只认「同级目录」时，从挂载 dmg 里双击必被 macOS 随机重定位（App Translocation）→ 同级没有载荷 → 线上报「未找到 install.sh」；改为「同级 → 挂载卷/下载/桌面」多候选搜索 + 载荷指纹 + 版本消歧，去掉不必要的可执行位检查，失败打印读数并加 `--print-payload-root` 诊断模式（可被门禁自测），客户安装手册随包分发 | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-setup-app-payload-locator.md) |
| ADR-0067 | 已发布的产物不允许被删除：发布即写仓库外归档（`~/Library/Application Support/LUTE/releases/`）并给两处副本加 `uchg` 锁定（`rm`/`mv` 一律 EPERM，唯一解锁点是重制时的归档改名）；判据 `release-verify.sh` 进门禁 `release-artifacts-intact`（清单在而字节没了即红灯），`release-restore.sh` 负责从归档或**外部副本**核验收回；不可找回者用 `release/<版本>.lost` 宣告（事实记录，不是豁免）。起因：产物两次从 release/ 消失且查不出人、本机无快照可回滚 | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-release-artifacts-immutable.md) |
| ADR-0068 | 授权状态必须同时读两个事实：TCC 一条授权 = 开关值（`auth_value`，隐私界面只显示它）+ 绑定对象（`csreq`）；换签后新 app 不满足旧要求而开关值原样留着 → 面板显示「已开启」而能力已死（本机实测 2.5 小时无人察觉，且 11:58 那次重授发生在仍是 adhoc 的 app 上 = 一次性代价花在了错误时刻）。判定唯一实现 `tcc-grant-status.sh`（3=死授权 / 4=判不了 / 0=含「尚未授权」），安装收尾强制调用并随包分发，指引必须写明「关掉再打开」，三处连线由门禁 `tcc-dead-grant` 守，检出器的反向自测（含恒真桩突变）由 `tcc-grant-status-selftest` 每次门禁跑 | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-tcc-dead-grant-detection.md) |
| ADR-0069 | 「输入监控」不是必需项：`post_events` 由「辅助功能」承载（2026-09-13 15:41 实测——库里**无** `kTCCServiceListenEvent` 行时 `doctor` 三项仍全 true，harness 源码零引用且自报 `input_monitoring_required: false`）。这与第一版错法（写「自动化」）同源：把未验证的事实写进出货面；**更坏的是门禁把它变成了强制校验**，六个出货面 + 单测一起钉死了它，谁改对反而判红。改为：必需面板两项、非必需项只可见不下结论、门禁禁把「输入监控/自动化」写成待授项（否定式说明除外）、`--format=tsv` 为唯一取数契约且机读列用 ASCII、「无记录 / 解不出 / 死授权」三态分开 | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-tcc-input-monitoring-not-required.md) |

| ADR-0070 | 技能「开没开」的真值只有一个家：**技能文件**。宿主级技能管理器合并注册表时，扫描到的条目保留自己回答过的 `provider` 与两个调用开关，注册表只能补 `whenToUse`/`title` 并贡献 bundled/runtime 独有条目——判据来自已定的优先级（扫描条目是同名冲突的胜者，胜者不该被落败者改写）。预设作用域内的 `dsh-skill-subset` 遮蔽（`hideOthers` 默认开）**仍然合法**，但不得冒充全局事实：实测它使面板对 77 张「文件说开」的卡显示关闭，而开关写的是文件 → 点一下亮一下又灭回去。同批：面向使用方的词必须自解释且**在页面上**给出解释（`chip.off` 定义了却从未渲染，等于没写），矛盾状态（本岗会带上 + 模型不会自动调用）必须在卡上明说 | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-skill-invocation-truth-and-vocabulary.md) |
| ADR-0071 | 复发故障总账：一天的 31 个提交按**根因**归并只剩不到十类，而其中三类当天各复发两次以上——教训全在 ADR/Note 里，丢的是检索面（复发按根因发生，文档按时间排列）。新建 `docs/pitfalls-playbook.md` 为唯一 home（四段：症状/根因类/已落地机制/下一版默认动作），**机制必须点名** `gate:<门禁名>` 或 `script:<路径>`；新增门禁 `pitfalls-playbook` 校验四段结构、点名机制在**实时**注册表中真实存在、编号连续、且 `AGENTS.md` 与 `docs/README.md` 都必须回引（没入口的总账等于不存在）——总账的固有死法是腐烂成一份说谎的清单，这副牙就是为它装的。同批新增 `docs-link-integrity`：实测 419 条相对链接里 6 条是死的且全是层级写错（同日同模板 5 篇 Note 错 4 篇），死指针把「这里没有依据」伪装成「有依据只是我没点」；两条校验各配含恒真桩突变的反向自测并进门禁（子进程走 `nodeCommand()`，否则 pnpm 下「退出码 0 且无输出」会让测试一条没跑也判绿） | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-pitfalls-playbook.md) |
| ADR-0073 | 出货面由白名单决定，不由构建机的目录状态决定；机器路径守卫必须能看见打包后的 tarball：`assemble.sh` §0 的整目录 `cp -R ~/.dsh/.agent-presets/.` 让「谁在打包机上新建一个预设，它就随下一版出给客户」成为一条静默路径——实测本机自有的机器人助理智能体预设 `bobo-cto` 自 2.3.0 起进入每版 payload（DMG 内 `skills-presets.tar.gz` 含 `presets/bobo-cto/`、出货 `completeness.json` 的 presets = 52 条含它）；同一版里守卫报绿而事实相反（内嵌 profile `✓ 无新增 37/39`，把出货 presets 解开再扫是 **103 个文件**含构建机路径），因为 tarball 是二进制、`grep` 一律跳过。产物改为：预设出货走白名单（未登记即中止并点名，数量不符/登记不实同样中止）、出货副本的构建机路径按映射表改占位符（未覆盖形态响亮失败，本机原件不动）、守卫加 `--tarball` 扫打包后的载荷；三条各配反向自测（含恒真桩突变与符号链接沙箱）进门禁 | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-shipped-preset-scope-and-payload-guard.md) |
| ADR-0072 | 岗位白名单是授予，不能被技能文件的开关否决：`dsh-skill-subset` 的正向注册是 `modelInvocable = respectFileFlags ? !disableModel : true`，而 ADR-0031 让语料 1338 张卡一律 `disable-model-invocation: "true"` —— 两条各自正确的决定相乘，白名单被它自己挂载的卡否决。实测改前 50 个岗位 439 个白名单位里 **275 个（62.6%）不可被模型调用，无一岗位全部生效**，且注册成功、无日志、界面两边正常。产物选「授予」：生成的 `agt-*` 改用 `respectFileFlags: false`（岗位装配由岗位负责，设置页开关管岗位之外），该值由生成器唯一常量决定；并加生成期「白名单生效性」判据（与既有「悬空引用」并列：悬空=名字不存在，死位=存在、注册成功、永不被挑中），有死位即红灯。判据管**不变量**（每个预设必须交付它声明的卡）而非旗标值，故本机自有的机器人助理智能体预设 `bobo-cto`（不在本项目打包与判据作用域）不动 | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-preset-skill-grant-semantics.md) |
| ADR-0074 | 技能出货面 =（被引用集 ∪ 产品级白名单）− 受限许可；落位树必须逐名等于选择结果：ADR-0073 排除本机自有的机器人助理智能体预设 `bobo-cto` 之后，它**唯一**引用的 15 个工程技能一并掉出（349 → 334；A/B 对照读数：本机 `PRESET_ROOT` 被引用 350 / 出货 `PRESET_ROOT` 被引用 335）。按当时的规则这是正确结果，但同族的工程工艺技能（`tdd`/`to-spec`/`to-tickets`/`write-spec`/`prototype`/`research`，挂 agt-009/012/013/048）本来就在出货面里——**产品拿到的是「一半工艺层」，而分界线取决于打包那台机器的引用图恰好长成什么样**：与 ADR-0073 同源，形态从「一个目录列表」换成「一张引用图」，两者都是机器状态、都答不了「产品要不要发它」。产物：新增 `packaging/shipped-skills.json` 只记**产品意图的增量**（不记全量清单，ADR-0009 仍成立）；白名单四种腐烂形态一律判否（名字不存在 / 与受限许可同名 / 缺 `why` / **文件缺失不得当空名单**）；`--check` 从「只看受限许可」升为「逐名等于选择结果」（静默少发与静默多发同罪，`--copy` 找不到源即中止）；装配与盘点都打印「救回未被引用几条 / 其中几条已冗余」；反向自测 S1–S9 + P1 + M1 进门禁 `shipped-skills-scope-selftest` | accepted（2026-09-13） | [Note](../notes/implemented/architecture/2026-09-13-shipped-skills-allowlist.md) |

| ADR-0075 | 发布前门禁只量「待发布」的对象：射程从 git 里的家推导，射程为空必须报「跳过」而不是「通过」。`gate:patch-anchors` 的红是**真的**（`staging/2.2.0`、`staging/2.3.0` 确实无 G1/G2），但把这条红当成「该打扫卫生」错在两处：锚点清单**单调增长**而 staging 是**累积的历史堆积**，两条各自正确的规则相乘 ⇒ 任何在「某条锚」出现之前装配的树**永久**判红，且这条红与「本次装配丢了补丁」在输出上不可区分（同一条教训 `verify-patches-v2.sh` 头部已写过一次：35 条 MISSING 与真漂移不可区分——同一根因第二次，P-11）；而且扫描集里的绿也不可信——`staging/2.3.1/app` 的守卫文件 mtime `14:07:15` 晚于该版入库清单记的 build `12:53:55`，**发布后被就地改过**，它的绿不证明任何出厂字节。更根本的是射程为空时本项静默报绿（`targets.length === 0 → passed: true`），而 `main()` 不渲染 `note` ⇒ 注释里写的「跳过，不假绿也不假红」与「都合格」在读数上同形。产物：射程判据落 `scripts/gates/patch-anchor-scope.mjs`（纯函数 + 7 个反向用例，含「无条件纳入」与「空射程恒置 false」两种恒真桩突变）= 本机 `/Applications` ∪ **未打 tag** 的 app 树（判「已发布」的家是 git tag，ADR-0058；已有 tag 的版本由产物负责，ADR-0067）；门禁读数分 `ok` / `skip` / `fail` 三态、`note` 必须渲染、跳过不计入通过数（`theme-tokens`、`worktable-fence` 接到同一状态）；**不删除任何 staging 树**——射程判据让历史树自动退出，删目录是结果不是手段。同批纠出一条此前无家的出货事实：随包锚点尺子逐版不同，2.2.0/2.3.0/2.3.1 载荷内是 36 锚点无 G1，2.3.2/2.3.3 是 38 锚点有 G1（**G1/G2 首见于 2.3.2**） | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-gate-scope-and-skip-state.md) |
| ADR-0076 | 发布链的**最后一环**必须有产出物：入库版本必须有分发面，跳过的版本必须被记录。2026-09-13 收尾对账发现前四环全齐（产物 + 仓库外归档 + `uchg`、入库清单、tag、两条远端推送）而第五环**从来没有过**——`gh release list` 最新是 v2.0.0，而仓库根 `README.md` 正告诉客户「从 Releases 下载」，**客户按文档操作拿到的是早三个版本的包**；2.0.1 / 2.2.0 / 2.3.0 / 2.3.1 从未在任何公开分发面出现过（实际渠道是 IM：2.3.1 的产物是从飞书收到的副本找回的）。这条缺口**没有任何一环会发现**：判据按**环节**写，而缺口在**环节之间**，且失败发生在仓库**之外**。产物：`scripts/gates/release-publish-scope.mjs`（纯函数 + 9 条反向自测）+ 门禁 `release-published`（`full` 模式；射程 = **既有** `release/<版本>.sha256`、**又有** `v<版本>` tag 的版本 = 两个家都在 git 里的交集，逐个核对 Releases 上是否存在且**非 draft**；`gh` 读不到时报 `skip` 而非 `ok`；取交集是刻意的——只有清单没有 tag 的 2.3.2 与只有 tag 没有清单的 2.0.1 都自动出局，既不需要「已评审不发」的例外表（第二个家，ADR-0009），也不会像「所有 `v*` tag」那样让 v0.1.0 永久判红（与 ADR-0075 同形））+ 门禁 `release-published-scope-selftest`；同批把 SOP §6 从 4 步扩到 6 步（加「推送两条远端」「发布到 Releases，含 `--latest=false` 与 draft 语义」「发布前必答两问：哈希复核 + 载荷敏感内容扫描」——DMG 内含 profile 快照而仓库是**公开**的，本次扫描两处疑似命中经定位分别是渗透测试技能的文档样例与 base64 编码的 WASM 字节码里的巧合序列）；**v2.3.2 不补 tag、不发 Release**——它在通过发布判据之前（载荷含错误的「第三项授权」说法）就被 2.3.3 取代了，补 tag 等于把一份已知带错的载荷说成发布版，它的缺席写进 2.3.3 的 release notes 与 `CHANGELOG.md` | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-release-published-surface.md) |

| ADR-0077 | 交付形态是**产物**的属性，不是**文档**的属性：卷内清单只有一个家，且由门禁与产物逐名对照。2026-09-13 复核 2.3.3 实物时把 SOP §5（终验）逐条对着磁盘跑了一遍，三条命令三处不成立——**同一根因的三张脸**：§5.1 的 `codesign` 指向 `packaging/release/$VERSION/DSH Desktop.app`（该目录只有 DMG 与三件清单，**没有解开的 app**，命令当场失败）；§5.2 断言「应看到 `DSH Desktop.app` 与 `Applications` 快捷方式」（实物是 `DSH Desktop.app.tar.gz` + `install.sh` + `LUTE Setup.app` + 四个 tarball）；§5.4 教用户「把 DMG 里的 app 拖到 `/Applications`」。三处都不是笔误，而是**上一版交付形态的快照**，而 2.3.3 从可拖拽安装盘换成了**离线安装器载荷**——快照不会因为产物形态变了而自己报错，于是所有门禁都绿（它们量的是补丁锚点、TCC 判据、文档链接、出货白名单，**没有任何一条把「文档断言的清单」与「产物真实的清单」比过一次**）。更坏的第二层：这条事实**本来有家而且那个家是对的**——`packaging/INSTALL-GUIDE.md` §2 的入口表准确描述了 2.3.3 的卷内清单，是 P-07（一条事实多个家只改了一个），**而漏掉的那个正好是复核者会读的那个**（P-01）。第三层是反向查出的：逐名对照时发现卷上有一个 `completeness.json` 从未登记，而它不是误入的临时文件（`assemble.sh` 第 650 行显式断言它必须写进 payload，`smoke-test.sh` 拿它比对清单）——**表是描述交付面的，而它是交付面的一部分，却在表外**。产物：卷内清单收成单一家（SOP §5.2 改为指向安装手册并不复述清单）+ 门禁 `dmg-layout-doc`（`scripts/gates/dmg-layout.mjs` 纯函数 + 15 个反向用例含**用缺陷原文当输入**的恒真桩突变；静态半 R1 单一家 / R2 形态断言永远可判，动态半 R3 未登记条目 / R4 幽灵条目 / R5 入口可点需产物在场，射程为空报 `skip`）+ 门禁 `dmg-layout-doc-selftest`；射程由 `selectLayoutTargets` **从 git 里的 tag 推导**——已打 tag 的版本退出扫描并在读数里点名，理由是下一版改手册会让所有挂载的旧卷**永久**判红（与 ADR-0075 同形；读不到 tag 时全部纳入 = 默认错误方向选「多量」）；安装手册第 2 节由「大约十个文件」升为可承诺的**完整清单**并补登 `completeness.json`；§5.1/§5.2/§5.4 三处保留「原来写的是什么、为什么会错」的原址说明。诚实划界：R2 是黑名单式的「廉价但精确」，挡不住下一句新的；§5.1 那种「SOP 里写着一个不存在的路径」本项**判不出**（通用路径判据会被 `.staging.XXXXXX` 这类故意不存在的路径淹没成误报），两处都在模块注释里明写「不假装它被拦住了」（P-03） | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-dmg-layout-single-home.md) |
| ADR-0078 | 运行中的字节不是磁盘上的字节：客户端 bundle 只有「重启」一条生效路径，且空射程的仪器必须自己报出来。2026-09-13 修侧边栏两处缺陷时，源码、构建产物、装载点逐字节都一致，而刷新页面后 AX 几何读数**逐位相同**。追下去是三个独立问题：①`@deepseek-ai/dsh-client-modules` 在激活时把每个包的 `client.js` 读成**不可变字节**并由 `compose()` 预算好 `URL → body` 供 HTTP 直吐，改写内存的唯一入口是 `rebuilt(id)`；而 `dsh-client-hmr` 的 production guard（`process.defaultApp !== true && process.env.DSH_DEV !== "1"`）在打包应用里**只推进基线并 return**，源码注释即「下一次完整重启才会从新字节重新组合」——**刷新页面在原理上就不是生效路径**，且基线被推进意味着该变更不会再被注意到；②③ `sync-profile.mjs --check --loadpoint` 与门禁 `profile-bundle-sync` 有同一个 bug：把 `file:` spec 的相对路径拿去对 **cwd** 做 `existsSync`，而 `file:` 的基准是**声明它的 package.json 所在目录**（profile）——实测 23 个 `file:` 依赖**全部**在此被 `continue`，门禁实际对比 **0** 个、恒绿；从 profile 目录跑则退化为比「profile 的 vendor 副本 ↔ node_modules」，两份都旧也互相相等。**而这条门禁的 remediation 原文就是「否则应用重启后仍跑旧字节」——它存在的唯一理由正是拦住此刻这件事**。决策：①生效路径只有「改→构建→tmp+mv 推装载点→**重启应用**」，刷新不是验收动作；②仓库源路径只能由包清单给出，不得由 `file:` spec 推导；③空射程必须自己报出来且与「全一致」不同形（判红 + 读数常显 `对比 N/M 个 file: 依赖`），沿用 ADR-0075；④修正射程后当场量出并修掉一条此前不可见的真实漂移（`dsh-overseas-skills` 三个文件装载点比仓库旧，本会在下次重启后表现为「改过的胶囊又回来了」）。诚实划界：**「运行中的字节」无法从磁盘判定**——本次能确认只因改的是可见几何，若改的是不可见行为则磁盘全绿而应用仍旧，无任何静态判据能发现，已在总账 P-15 明写为「判不出」 | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-running-bytes-are-not-disk-bytes.md) |
| ADR-0079 | 同一列里的注入式侧边栏行必须声明同一条行轴；一致性由「清单 + 显式期望值」守，不由纪律守。2026-09-13 修完侧边栏两处缺陷后重启复测，发现**同一个缺陷只修了一半**：当天先修的是字段可见的「技能中心文字居中」（`text-align` 是继承属性，`<button>` 的 UA 默认 `center` 继承到 `flex:1` 的标签 span，而同族的岗位矩阵声明了 `left`），修时留下的注释**准确写出了根因并引用了 P-07**，几何那半却原样留着——两行并排、同一形态、`role-matrix` 用 `width: calc(100% - 8px)` + `margin: 2px 4px` + `padding: 6px 8px`（content-box），`skill-center` 用 `box-sizing: border-box` + `width: 100%` + `padding: 0 10px`，AX 实测行框 **68…316 与 64…320**（原生侧边栏行轴）。「记住了根因」不等于「有一处机制在对照」，属 P-07 + P-08。同一场取证还**推翻了自己的分组**：把底部「深度研究」与导航两行并成一组会得出相反结论——它走官方 `sidebar.footer.action` **slot**、不是 DOM 注入，实测 62…314，**与同节原生「知识库」逐像素重合，与导航列不同才是对的**；据此确立「判据按列分组，不按看起来像不像」。决策：①同列同行轴——水平轴元组 `box-sizing` / `width` / 水平 `margin` / 水平 `padding` 必须相同，垂直轴（高度、行距）**不在判据内**；②**期望值是声明出来的**，每列显式写下一个期望轴（导航列 = 原生行轴），**禁止「拿第一行当基准」**——那种写法在第一行本身就错时恒绿，正是 P-02；③**清单本身即判据**（ADR-0069 形态）：未登记的注入行、登记项指向不存在入口、`position` 与清单不符，三者都判红，所以清单不会腐烂、新行不会静默逃逸；④**不抽共享样式包**——两个包各有品牌与交互细节，在 `file:` 装配下只会引入**新的**需要同步的副本（用一份新副本取消两份旧副本是净亏），本 ADR 只把一致性机器化；⑤**射程与边界自己报出来**（沿用 ADR-0075）：读数常显 `已登记 N 个注入行、M 列；其中 K 行断言了行轴`，射程为空判红而非报通过，底部官方 slot 的行**明确不纳入**并写明理由（`split` 列那半豁免已于 2026-09-18 由 [ADR-0124](ADR-0124.md) 撤销：`split` 实现删除、列退役为 `nav-band`，且**被插件改写的官方行**首次进入断言面）。实现用**构造证明**而非算术：两行是同一父节点下的同一个 `<button>`、经同一共享核心取同样的类名，故只要那几条规则相同几何就必然相同——构建产物逐属性对照确认只余 4 处差异（`color` fallback 拼法、`font: inherit`、2px 纵向 margin、`nowrap` 住在 `.entryLabel`），**无一落在水平轴上**。产物：`scripts/gates/sidebar-row-axis.mjs` + 13 条反向自测（含恒真桩突变：只核对「登记了没有」的实现必须失效）+ 门禁 `sidebar-row-axis` / `sidebar-row-axis-selftest`；并在**真实树**上端到端验过它会咬人（改回缺陷形状当场报出三条违规，随后逐字节还原、sha256 前后相同）。诚实划界：判据**只读声明文本、不读渲染结果**，挡不住「声明一致而渲染不一致」；几何生效仍需**重启应用**（ADR-0078） | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-injected-row-axis-single-home.md) |
| ADR-0080 | 已证伪的仪器不得再被开成判据；「在不在跑」收成一家，且「读不到」必须与「没有」分开。2026-09-13 复核发布前清单时读到一个**空读数**：`docs/sop/dmg-release.md` §0 让操作者确认「本机无运行中的 DSH 实例」，判据是一条 `pgrep -f`，而**主进程确实在跑时它返回 0 条**（`ps -Ao pid=,comm=` 给出 `87357 /Applications/DSH Desktop.app/Contents/MacOS/DSH Desktop`；同时刻 `pgrep -f 'Contents/MacOS/'` 命中 222 条但**不含**主进程）。阳性对照把范围钉死：把 `/bin/sleep` 拷成 `…/Contents/MacOS/DSH Desktop` 并启动，**同一条模式当场命中**，杀掉后归零——不是 pgrep 不好用，是它对**这个** Electron 主进程盲（原因未查明，故本条记录的是读数不是机制）。失效方向是最坏的一种：清单返回空 → 复核者得出「没有实例在跑」 → **在运行中替换 app bundle**（宿主 HMR 热更 → 整屏白屏）——**它守的白屏红线等于没有守**（P-02）。更贵的是形状：同一条事实 2026-09-12 就写在 `first-launch-test.sh` 的注释里（连「原因不明」都写了），没变成机制，于是同一条判据又在安装器 0b / 首启验收 / 品牌重放 / 发布前清单四处各写一遍，**修的是唯一被看见的那一处**（P-07），而漏掉的正好是操作者照着敲的那份（P-01）。决策：①「在不在跑」只有一个家 `packaging/scripts/dsh-running.sh`（随包分发到 `tools/`），四处消费者一律调它、不重写；②退出码契约 **0 在跑 / 1 没在跑 / 2 用法错误 / 4 判不了**，调用方拿到 4 必须**中止**——安装器因此新增硬失败：`ps` 读不出时**拒绝安装**，「读不到」不再能伪装成「没有在跑」；③判据读 `ps -Ao pid=,comm=`（可执行文件全路径，不扫 argv：既精确、又不会被调用者自己的命令行误命中），`--any` 尾锚定认 bundle 名供首启验收问「有没有任何一份在跑」；④死仪器登记进 `scripts/gates/dead-instruments.json`（每条必须带 `provenOn` / `reading` / `why` / `useInstead`）+ 门禁 `dead-instrument`：射程＝git 索引里的 `*.md`/`*.sh`/`*.bash`，**判据面只算会被照着跑的三种形态**（清单行及其续行、围栏代码块、脚本里去掉注释后仍有内容的行），`「…」` 引用不算使用，读数常显分母，**空登记簿 / 字段不全 / 取不到射程 / 判据面抽出 0 行一律判红**——「一个都没比」不能长得像「都比过且干净」；⑤SOP §0 那条改为**指向判据本身、不复述命令**。产物另含 `dsh-running-test.sh`（11 条：真进程阳性、同路径同字节的阴性、判别力对照、`ps` 桩→4 而非 1、受控进程表钉 `--any` 尾锚定、两处恒真桩突变）与 `installer-running-guard-test.sh`（按字节抽出安装器 0b 块配桩跑，钉 T1~T4 + 突变；不跑整个安装器——那会替换开发机上真正的 app）。副作用：本次实现**当场被本仓库已有的判据咬了一口**——门禁 `shell-var-multibyte`（ADR-0064）报出我新写的三处 `$VAR` 紧跟全角括号（bash 会把多字节字节并入变量名，`set -u` 下直接中断），已改 `${VAR}`；本 ADR 引用缺陷原文的围栏代码块也被**它自己提出的**这条门禁判红三处，改写为 `「…」` 引用后通过。诚实划界：`pgrep` 为何盲未查明；判据只回答「有没有」不回答「是不是同一个 app」；**散文里的死仪器判不出**（机器分不出叙述与规定，扩到散文会制造假红），在模块注释里明写不假装被拦住 | accepted（2026-09-13） | [Note](../notes/implemented/contract/2026-09-13-dead-instrument-prescription-binding.md) |
| ADR-0081 | 图标有两个家：Finder 图标对了，不等于 Dock 图标对了。`brand-replay.sh` 长期只守 `Contents/Resources/icon.icns`（**Finder** 那张），而 **Dock 与托盘**由 `dsh-plugin-desktop` 启动时 `app.dock?.setIcon()` 用 `app.asar.unpacked/build/{app-icon-mac.png,app-icon.png,tray-icon*.png}` **覆盖**——那一套从未被品牌化（读数：官方 `app-icon-mac.png` 2.2 MB vs ROOT 图标 47 KB），于是「Finder 里是 ROOT、Dock 里是 DSH 原生」与脚本的 `BRAND ALL VERIFIED` 同时成立（P-02 仪器假绿 + P-07 一条事实多个家）。决策：①运行时图标资产入库 `packaging/assets/brand-icons/`（7 个 PNG；与 `app-icon.icns` 同一模式——真相源在 `.dsh-root-brand-preview/root-icon/` 不进仓库，仓库放可分发副本）；②`brand-replay.sh` 第 5 块用一张 `ICON_PAIRS` 三列表（**目标 : 资产 : 尺寸**）同时管核对与落笔：`--check` 报 `DRIFT`、`--apply` 落笔前量**目标**尺寸并拒绝不符的、**资产缺失判红而不是静默跳过**；③`assemble.sh` 随包分发并以 `BRAND_ICONS_DIR` 把仓库侧资产显式传给重放脚本（不让同一批字节在仓库里存第二份）；④门禁 `brand-icons` 守那张表的**第三列**——`--apply` 量的是目标，**资产自己错了没人管**（512×512 的资产配 1024×1024 的声明：目标与声明相符、`cp` 照落、Dock 图标成一张放大的模糊图而**所有读数全绿**，与 P-02 同一形状：仪器量错了对象），判据为表 ↔ 资产目录**两向**逐名对照 + **资产实际像素 = 声明像素**（直接读 PNG 的 IHDR，不用 `sips`/`file`——它们不可用时判据会静默变成「没什么可比的」）+ 目标名唯一 + 动态半核对已装 app 的目标文件名，读数常显分母、目标侧不在射程时报 `skip` 而静态半照常说话；⑤`refresh-app-brand.sh` 把品牌补落到**已安装**的 app 并**同身份重签**（`app.asar.unpacked/**` 在封条之内——就地改 = seal 破损 = TCC 授权被静默拒绝，ADR-0068；指定要求是 `identifier + certificate leaf`，与字节无关故授权延续，ADR-0063），其动手前的闸改用 `script:packaging/scripts/dsh-running.sh`（ADR-0080）——**它原来的 `pgrep -f` 版本在 DSH 运行时没有拦住，`--apply` 照跑了下去**。验证：`--check` 报 9 处 OK（`icon.icns` + 8 个运行时图标）；DSH 运行中跑 `--apply` 退出码 **3**、在任何写入之前拒绝、图标字节 sha256 前后相同；`brand-icons` 反向自测 11 条全绿（含 M1 恒真桩突变：只比「声明 vs 声明」的实现会放过资产尺寸错那一类）。诚实划界：**Dock 上现在显示哪张图没有任何静态判据能回答**（由 LaunchServices 缓存与 `setIcon()` 运行时调用共同决定，同 ADR-0078 的边界）；本项不核「图案对不对」，只核尺寸与清单；`.icns` 那一半家不在 `ICON_PAIRS` 表内 | accepted（2026-09-13） | [Note](../notes/implemented/surface/2026-09-13-runtime-icon-second-home.md) |
| ADR-0082 | 精选线接入分类底本：只增不改、阻断可见，且工具自己的断言必须先有牙。`staging/<L2 域>/<slug>/SKILL.md` 是产品侧卡的**实物**，`data/classification.json` 是它的**分类底本**；两条判据从相反方向指着同一件事——`verify-install.mjs` 把「占用 `p2s-` 前缀但底本里没有」的目录判成**编外目录**（exit 1），S12 消费口闸门把「契约引用了精选线 id 但技能目录里没有」记成 `待装线`（实测 14 条）。本次落地换底：`classification.json` 1338 → **1390**（+52）、`card-facets.json` 随动重算、`manifest/paper2skills.json` 进目录，新增唯一 writer `scripts/append-selected-line.mjs`（输入只有 vault 的 `card-classification.json` 与 `lib/taxonomy.js` 的判据实现，**不新造分类事实**，每条新增带 `_provenance` 可逐条回溯）。真正的发现不在数据而在 writer 自己的判据上——两条纪律断言**都是恒真的**（P-02）：①「只增不改」的守卫是一段**空循环**（注释宣称「逐项确认没被改过」，循环体什么都不做、从不往 `problems` 写东西）；②`--check` **无条件**打印「✅ 计划与现场一致」——把底本回退到接入前（52 条待接入）实测仍 ✅ + **exit 0**。决策：①只增不改改成真判据并**先量后写**定口径——只比两侧 schema 都有的 `l3`/`confidence`/`note`（不比 `title`：底本存卡面长标题、精选线只有 id 短名；不比 `facets`/`planes`/`domains`：由 `resolveFacets()` 派生的展开量；不比 `slug`：由 `slugFor()` 派生），实测射程 **93 条**、`l3` 93/93、`confidence` 93/93 逐字一致，`note` 81 条逐字相同 + 12 条「空 ↔ 占位说明」按**同一事实的两种写法**规范化、但把「规范化了几条」当**读数**打印，**射程为 0 单独判红**（P-11：空射程不得长得像通过）；②`--check` 有待接入即判红（正向 0 条→exit 0，反向 52 条→**exit 1**）；③第 53 张卡（`Skill-大规模消费者评论方面情感分析` 被 `slugFor()` 折成非法 `p2s-`）登记为**可见阻断**、不改名——在产品侧改名 = 自造第二套 slug 规则，S12 的 id→slug 换算会算错；本次达成 **52/53**，账如实留着；④`data/venue-tiers.json`（S10 投递副本）的真实状态是**已投递、待接读**：writer 是仓外 `build_venue_tiers.py --sync-product`，J9 逐字节断言与 T9/T9b/T9c 三条自测都在该脚本里（实跑全判据通过），但产品侧 `lib/axis.js` 仍是**硬编码**、还不是它的 reader；且两处**已分歧**（副本 `tier_alias` 已定 `workshop/demo/findings/short-paper → preprint`，`axis.js:58` 仍写 `to: null`「留给 S10 决定」），接线时两处必须一起改。诚实划界：换底**未达 53/53**；`venue-tiers.json` 尚未生效；S12 的 13 组/27 条变异自测**不在门禁射程内**（P-04，另行立项）；`generate.mjs:73` 与 `lib/contract-gate.js:28` 指向不存在的 `scripts/check-contract-gate.mjs`（P-09） | accepted（2026-09-13） | [Note](../notes/implemented/capability/2026-09-13-curated-line-append-and-two-false-greens.md) |
| ADR-0083 | frontmatter 重写必须保留键的原位：字节不变是开关的承诺，而键序看不见。`lib` 里唯一写 `~/.dsh/skills` 的地方是算法技能页的开关，承诺写在它自己的注释里——**点两下 = 一个字节都没变**。原实现把 `disable-model-invocation` / `user-invocable` 从块里**摘掉再追加到块尾**，这在「键本来就在块尾」的卡上完全正确，而那正是组装器（`assemble-skills.mjs:173-175`）产出的形状、也就是写这段代码时看得见的形状；但带精选线换底字段的卡把开关排在**中间**（`rebase_*` 由上游 vault 侧**后来追加**，追加发生在开关之后）。实测全量 1390 张：无 `rebase_*` 键 1245 张字节不变 ✅／有 `rebase_*` 键 145 张**每张都漂移** ❌，漂移形态纯粹是键序（`… enabled / disable-model-invocation / rebase_*` 回来变成 `… enabled / rebase_* / disable-model-invocation`）；全语料普查确认形状只有一种（开关在前 145／在末尾 **0**／无该字段 1245），即漂移集**恰好等于**含 `rebase_*` 的那 145 张。它能活下来的原因是**键序没有任何读者**——不进任何解析结果（解析出来是一张 `Map`）、值原样往返、页面读回来状态完全正确，唯一看得见它的是字节比对；而那条字节比对**按语料**验证，在没有语料的机器上整个 describe 会跳过（该文件注释自认「worth nothing on a machine with no cards」），纯函数单测里又**唯独缺「键在块中间」这一形状**——缺陷正是从这个空档漏过去的。责任归属用读数定：`corpus-write.spec.ts` 与 `frontmatter.ts` **只在 `b0ba3d5`（2026-09-12）进过一次**、之后再没被任何提交改过，145 张卡落盘于 `2026-09-13 21:15`（换底安装跑）⇒ 语料先变，缺的是**没被跑到**的那个测试（`scripts-runnable` 是 `full` 模式项，而 `gate:full` 此前一直因别处报红）。决策：①`rebuildFrontmatter` 改为**保留原位**——已有的键就地改写、缺失的键追加到块尾、重复的键收敛到**最后一次出现**的那个槽位（加载器读的就是那一次），「替换而非追加」的语义不变但替换发生在原位；②补三条**纯函数**单测（键在块中间／重复键收敛到最后的槽位／只缺一个键），不依赖本机语料，把守卫从「环境依赖」变成「CI 与全新检出上同样有效」；③同批修 `lib/contract-gate.js` 的 5 处 typecheck（补 `readContracts` 的 `missing` 与 `computeLedger` 的 `l3` 真实字段声明，其中 `bySlug` 是**改错声明**：原写 `{contract,raw,kind}` 而实际只 push `{contract,raw}`——类型在说假话，补齐后 TS 立刻指出矛盾，遂把 `?? bySlug.set(...).get(...)` 换成先取后判空，不在类型里写假兜底）。否决的备选：把「字节不变」放宽成「值不变」（拆掉唯一的守卫）、重排 145 张卡的键序（语料是上游产物的实物，本仓库没有 `rebase_*` 的写入器，下次重装会洗掉）、只靠语料测试（缺席即跳过）、只改注释接受漂移（没有读者的不变量会变成又一次静默改写）。验证：修复后全量语料 `UNCHANGED=1390 / DRIFTED=0`、包内 `154 passed`，并做了**双面验证**——把「原位改写」退化成旧的「追加到末尾」后 **13 条红**（含新增的三条纯函数用例），还原后 26/26 绿。门禁 `gate` **49/49**、`gate:full` **55/55**。诚实划界：块内**混用行尾**时仍按首行风格归一（既有残余，本次未变） | accepted（2026-09-14） | [Note](../notes/implemented/surface/2026-09-14-switch-rewrite-key-position.md) |
| ADR-0084 | 出货 preset 的行级闭合判据：判据的事实必须来自**出货面**，不是开发机此刻的状态。客户在第二台机器上装完 2.4.0 后打开 DSH，**「结伴 · 达人与联盟合作」（`agt-033`）这个 preset 加载失败**。实测成因：payload 的 `presets/agt-033/agent.cordis.yml:168-169` 带着 ADR-0061 的**本机装配**行 `- id: product-kol-hunter / name: 'dsh-kol-hunter-local'`，而同载荷的出货 profile 里没有这个包（`dsh.profile.bundles` 无它、`node_modules` 无目录）——行在、包不在 ⇒ 该行解析不到 ⇒ 整个 preset 加载失败。更值得修的是「为什么没被拦住」：已有的两道判据都是**反向特征**——`select-presets.mjs`（ADR-0073）管「哪些**目录**出货」（`agt-033` 本来就该出货，射程外）；`strip-local-products.mjs` 先在**本机 profile** 的 `file:` 依赖里算出「外部产品名」再拿这个名字去删行。而 2.4.0 装配那一刻（09-14 11:2x），本机那条依赖与 bundle 早已被 09-13 的安装抹掉，只剩 preset 里这一行 ⇒ `extNames` 为空 ⇒ 脚本**如实**报告「✓ 出货面没有本机装配的外部产品」，那一行大摇大摆出了门。同一份出货副本、同一台机器、只换本机 profile 的状态，判据给出**相反的结论**（两次 `--dry-run` 实测：用 09-13 的 profile 备份 → `移除 补丁行: agt-033/agent.cordis.yml: - id: product-kol-hunter`；用装配时那份 profile → `外部产品 0 个（无）`）。同族盲区：`dsh-preset-lint-local` 对这份坏字节报 `agt-033/agent.cordis.yml OK（2 警告）`——它校验组合词汇的静态形态，不含「这一行能不能被解析」。决策：①新增**行级闭合**判据 `packaging/scripts/check-preset-rows.mjs`，事实来源换成**出货面**（出货 profile 的 `node_modules` ∪ app 内嵌 `node_modules` ∪ 登记的 `builtins`），判的是「产物自己的性质」而不是「开发机此刻的性质」；已登记为本机装配的行从**出货副本**剥掉（连同紧贴其上的注释）并逐个打印读数，**登记即权威——哪怕本机此刻真的装了这个包也照样剥**；未登记又解析不到的行 → **装配中止**并点名到「文件 + 行 + 包名」；解析面 / 登记处读不到、登记缺 `why` 一律 `exit 2` 响亮失败（过期的登记只告警不判红，与 ADR-0073 的 `exclude` 同规）；②登记处 `packaging/local-only-preset-rows.json`（`builtins` + `localOnly`，两条出路都写成可评审的表态）；③挂在 `assemble.sh` 的 **tar 之前**（判的就是即将打包的那份字节）+ 门禁 `preset-rows-resolvable-selftest`；④块切分与过滤收进 `packaging/scripts/lib/yaml-rows.mjs` 与 `strip-local-products.mjs` 共用（原先只有一份实现，新判据需要同一套切分——抄一份就是 P-07；重构的行为保持证据是两个夹具上 `--dry-run` 输出**逐字节相同**）。这是 ADR-0073/0074 那套语法的**第三层**（目录级 → 技能级 → **行级**），形状一致：出货面由显式表态决定，未登记 = 没人表过态 = 停下来问。验证：判据在**真实的 2.4.0 字节**上跑 51 个预设 / 1582 条行 → 恰好 1 条违规（`agt-033:168`）、零误报；掏空登记 → `exit 1` 点名；`--strip` 落盘 → 只动 `agt-033` 一个文件、顶层行 19 → 18、`diff -rq` 其余全等、再判 1581 条全绿；自测 15/15，恒真桩突变下 S2 失效。诚实划界：解析面是**目录存在性**不是 Node 的模块解析算法（`exports`/`main`、目录名 ≠ 包内声明名都不查，后者会响亮失败而非静默放行）；**已知缺口**——`install.sh` 第 5/6 步对 presets 用 `cp -Rn`（不覆盖已有），所以**已装 2.4.0 的机器即使拿到干净的下一位版本也收不到修正**，需另立决策（改安装器语义或人工修复，Note 里给了一条命令） | accepted（2026-09-14） | [Note](../notes/implemented/contract/2026-09-14-preset-row-closure-vs-dev-machine-state.md) |
| ADR-0085 | 归位 ≠ 接线：技能入库必须分两个平面各有一条判据，且判据的前提要能分辨「环境不在」与「数据不对」。T0 精选 15 条通用技能已装好、四件套齐全、运行时前提齐备、设置页可见——**所有既有判据全绿**，而按设计应当常挂全部 50 个岗位 preset 的它们，在 `generate.mjs` 生成的 `skill-subset` 里**一条都没有**：对会话而言等于不存在，且没有任何一处报错。成因是「归位」（技能属于哪些岗位、喂给哪个页面）与「接线」（preset 实际挂载、喂给会话）共用一个动词，于是可以整套做对其中一半而另一半完全不做，两边读数都是绿的（P-03/P-04/P-13 同族）。同一轮还量到：**`pnpm run gate` 里从来没有一项跑过 `verify_static.mjs`**——SOP §4 与 `pipeline.sh` 一直写着要跑，但它只有人工执行这一条路；以及 `verify-fullstack.mjs` **恒红**，它把「预设组不在本机」（v3 重组时 `ai-product-developer` 已移出，现存只有 `agt-001..050` + 两个私有 preset）报成「**预设副本丢失**」——前者是环境事实、后者是数据缺陷，同一句话把两者说成一样，于是这条红既不能驱动「重装 preset」也不能驱动「查谁改了数据」，而它是 2026-09-12 一次改动里显式挂账「留待单独处置」后无人处置的债。决策：①技能落点分两个平面，各自独立判据，且判据只读**消费侧产物**（会话可见的白名单），不读登记表或生成器意图；②新增第三条线「通用技能」（`genericNames`），T0 档进全部 50 个岗位 preset，归位写入 `manifest/generic-skills.json`（**唯一一份**，纯派生自 `staging/intake-localize.json` + `manifest/intake-provenance.json`，派生器 `scripts/build-generic-manifest.mjs`，**T0 名单的家是派生器里的 `T0_NAMES`**，其余四处读它），接线并入点放在**契约闸门之后**——闸门判的是 `p2s-` 卡的契约引用，第三方通用技能没有契约可挂，放闸门之前它们在 `P2S_CONTRACT_GATE=enforce` 下会被**静默删掉**；接线判据读**回落的字节**（写盘后重新读回 `agent.cordis.yml` 逐条核对，缺一条 `exit 1`——只报「名单有 15 条」是自述不是读数）；③新增 `gate:skill-lines` 把三条线的验证器收进 `pnpm run gate`，射程前提统一为「本机有没有 `~/.dsh/skills` 与 `~/.dsh/.agent-presets`」，两者都不在 → **跳过并写明跳过了什么**（不假绿也不假红，与 `patch-anchors`/`theme-tokens` 同语义；恒红的判据与恒绿的判据一样没有信息量，人只会学会绕过它）；④`verify-fullstack.mjs` 的预设副本判据改为「目录不在→跳过并说明；目录在→逐条核对」，期望条数**改从 `presets/preset-skills.json` 读**而非硬编码 3（硬编码让「少了一条」与「改过清单」无法区分）。附带收益：`taxonomy-v3.json` 的 `overseasNames`/`fullstackNames` 从**无人校验的死数据**（写进 SOP §12 却无消费者）变成活的——一条判据同时锁三个名单。验证：`pnpm run gate` 55 项；接线的 diff 是确定性的——50 个 `agt-*` 每个恰好 +15、1 个非 `agt` preset 不变、**移除 0 条**、新增并集**恰好等于** T0 名单、总条目 474 → 1224（= 50×15）。诚实划界：T1（按岗位族挂）本批为空，`generate.mjs` 会把非 T0 成员打印出来明确说明**未被接线**；每会话技能目录多约 1,900 tok 是 T0 常挂的必然成本 | accepted（2026-09-15） | [Note](../notes/implemented/capability/2026-09-15-generic-line-wiring-and-skill-lines-gate.md) |
| ADR-0086 | 「装了」不等于「跑得起来」：第零步判的是「这台机器现在能不能跑」，不是「来件长得对不对」。既有 SOP §12 的全部判据（目录名唯一 / 图标覆盖 / 路由无悬空 / frontmatter 齐全）在回答同一个问题——「引用的 skill id 存在吗」，没有一条在问「跑得起来吗」；实测 T0 15 条里 **4 条装完必然 ImportError**（缺 `openpyxl`/`matplotlib`/`python-docx`/`python-pptx`/`PyMuPDF`/`pdfplumber`/`pypdf`/`reportlab` + Node 侧 `vega`/`vega-lite`/`sharp`），而当时**所有门禁全绿**——卡片正常、`installed=true`、模型看得见（P-02）。同轮两种更隐蔽的形态：`matplotlib` **装上了**但默认 `DejaVu Sans` 不含 CJK 字形 → 中文标题渲染成豆腐块，**脚本退出码 0、图也生成了，坏在交付物里**；以及不保留 `.git`（§12.2）之后「从哪来、哪一版、被改过什么」没有任何记录，升级比对与回滚同时失去依据。决策：①运行时前提独立成「第零步」，抽取产物 `manifest/runtime-deps.json` + 人工核对覆盖 `runtime-deps.overrides.json`（**替换不是合并**——合并会把已判定为误报的条目带回来），抽取必须**按代码块语言分流**（实测三次误报全出自此：`convert .doc to .docx` 被当 ImageMagick、CSS 术语 `dot` 被当 Graphviz、`\s+` 吃换行把下一行 `node /data/...` 吞成包名）；②依赖进 `uv` 受管 venv `~/.dsh/skills-runtime/.venv` 钉 **3.12**（非本机 3.14.7：PyMuPDF/scikit-learn/scipy/pandas 的 macOS 轮子在 3.12 上齐全），**不放 `~/.dsh/skills/`**——那里每个子目录都被当成一条技能，会污染 `verify_static` 的悬空引用判定；两条会静默出错的细节：探测口径必须跟着 venv 走（否则「系统有 pandas ✓ 而 venv 里没有」→ 门禁绿、一跑 ImportError）、installer 必须先建 venv 再探测（第一版顺序反了，漏了 scipy/scikit-learn，跑第二遍才收敛）；③环境前提也要有判据且必须**真渲染一次**：CJK 配置落 venv 内（`sitecustomize` 设 `MPLCONFIGDIR`；不放 `~/.matplotlib` 会作用于系统与 DSH 自身、matplotlib 3.11 已不搜 `$VENV/etc/matplotlibrc`、改 site-packages 会被 `uv pip install --reinstall` 抹掉），自检用 `-W error::UserWarning` 把缺字警告升成异常——**不可用「字体文件存在」代替**，因为这类缺陷的症状正是「退出码 0、产物是坏的」；④来源留底独立成产物（`manifest/intake-provenance.json`：批次 / 原始单元 / **逐文件 sha256** / 许可证结论 / 已应用修补 / 已知缺口），修补与豁免必须声明并沿用 ADR-0014（匹配不到缺陷的豁免报错），回滚用 `ditto` 禁用 `cp -R`（实测会拍平目录）；⑤**局部扫描写 manifest 必须并入不能覆盖**——实测一次试跑把 manifest 从 5 条缩成 2 条，门禁不再检查另外 3 条**继续报绿**（P-11 射程被悄悄缩小）；⑥门禁 `gate:skill-runtime-preconditions` 的射程跟着**接线**（`agt-NNN` 的 `skill-subset`）而不是清单：未接线的技能不拖红，一旦接线就必须齐备，**只有没有任何已接线技能声明 Python 依赖时才允许跳过**，探查器吐不出可解析读数同样判红（P-17）。两处被实测推翻的原始判断登记更正去向：`chart-gen` **不需要 matplotlib**（它是 Vega-Lite + Sharp 的 Node 实现，原判断来自只读描述）、T0 的 `humanizer` 换成 `humanizer-zh`（原件的 439 行规则基于英文语料，挂 50 个中文岗位会误判）。验证：受管 venv `Python 3.12.13`；结构体检 592 来件实测**致命 12 / 告警 144**（第一版规则误报 135 条，收窄判据后收敛）；四条带重型依赖的技能做**真实运行取证**（`chart-gen` 出 PNG 690×390、`sn-da-excel-workflow` 写读 xlsx 带条件格式、`sn-da-non-spreadsheet-analysis` 解析 docx/pptx/pdf、`minimax-pdf` 出中文 PDF 并回读文本）。诚实划界：只保证依赖**就位**，不管版本合适与否、不管技能逻辑与产物内容；本批只对 T0 做过完整第零步，剩余来件的 `missing` 尚未逐条探测 | accepted（2026-09-15） | [Note](../notes/implemented/capability/2026-09-15-intake-runtime-preconditions-and-provenance.md) |
| ADR-0087 | 设置页呈现层：锚点取 **ARIA 语义**而非类名解析，分组是**登记制**且「不连续」必须拒绝。实测三处根因：①官方 `width:800px` + `max-width:calc(100vw-48px)` **有**小屏保护、缺大屏扩张（1580px 窗口里面板仅占 60.6%）；②导航 **18 项需 852px、面板可用 752px**，官方 `nav` 无 `overflow`、`panel` 是 `overflow:hidden` → **最后两个设置页被裁成 1px 高、完全不可达且不报错**（这是功能缺陷不是观感问题）；③面板背景取 `--dsw-alias-bg-layer-2`（实测 #342f30），导航区与内容区背景完全相同，品牌绿 `#58B848` 在设置页一次都没出现，而官方 `brand-primary` 其实是中性近黑 `#0f1115`。分组的难处不在样式在契约：`settings.section` 只投影 `id`/`order`/`label`，**没有 group 字段**，shell 又是「零自有文案的纯组合面」。决策：①新建 `dsh-settings-shell-local`，分 L1 样式层（纯 CSS：`nav` 加 `overflow-y:auto` 修不可达；宽度上限 800→960px、纵向边距 48→32px；导航列 4% 文字色派生底 + 1px 分隔；品牌色**只做点缀**——当前项左侧 3px 指示条 + 焦点环，浅色用加深变体 `#3d8a33`（白底 4.30:1）深色用原色（5.24:1），因原色在白底上只有 2.51:1 不达标）与 L2 分组层（读 `ctx.slots.entries("settings.section")`——契约明写返回顺序即渲染顺序，**不猜 DOM 顺序、不匹配本地化文案**）；②锚点**全部取自官方 DOM 的 ARIA 语义**（`[role=dialog][aria-modal=true]` 且直接子元素含 `<nav>` / `nav` 内 `button` / `aria-current=true`），**一个类名都不解析**——比项目既有的包路径锚少依赖一层「模块文件名」，也避开同一机制写第二份实现的分叉（P-07），列表容器由按钮**反推**共同父元素而不写 `div:last-child`，产物中出现哈希形状选择器由 `validate-build.mjs` 判红；③三态读数分开：`absent`（没打开，正常）/ 成功 / `drift`，且 **drift 只在曾经成功解析过之后才可能触发**——别的 modal 没有 `<nav>` 是正常的，一律报漂移会让读数变噪声（P-02）；④分组三条拒绝条件（宁可没有分组也不错着显示）：条数 ≠ 按钮数不注入、**某组被别的组打断不注入**、期望状态已成立则**一个字节都不写**（观察器监听整个 `body`，写就自激）；第二条是实际踩到的——order 28 上挂了两个 section（`generic-skills` 与 `wanzh-hulian`），按语义最优划分会让 `algo-skills`(29) 显示在**「扩展」标题之下**，归属错位且看起来完全正常，故分组表按「每组在渲染顺序里连续」重划并把连续性做成**判据**而不是纪律。验证：41 条用例全绿、门禁 56/56、仓库源 ↔ 装载点逐字节一致。诚实划界：活页面 `grouped:5` 读数**尚未取得**（生效需重启应用，而重启会中断当时会话）。 | accepted（2026-09-15） | [Note](../notes/implemented/surface/2026-09-15-settings-shell-reachability-and-grouping.md) |
| ADR-0088 | profile 清单漂移必须在 **vendor 与装载点两侧都有射程**，且「比了 0 个包」不得读作「都一致」。起因是一条**假声明**：`dsh-settings-shell` 的 `dsh.client.inject: ["@deepseek-ai/dsh-client-ui-slots"]`（连同同名 `peerDependencies`）长期没人发现是假的——读官方装载器（`dsh-client-modules` 的 `arriveGraphRow`）才看得见它的语义是**失败即降级**（`row.inject` 里解析不到的包名直接跳过），于是它**既不报错也不生效**：产物里一个 `require` 都没有（`react` 出现 0 次，整包自足），而那个包在本机任何地方都解析不到。删掉它才暴露出第二个问题：**这条清单改动传不进运行时会读到的地方**——`sync-profile.mjs --apply --only-metadata` 与门禁 `profile-metadata-sync` **都把 vendor 路径写成了扁平的 `vendor/<包名>`**，实际布局是 `vendor/packages/<组>/<包>`，25 个受管包**一个都命中不了**，`checkProfileMetadata` 的 `existsSync` 把每个包都 `continue` 掉 → 门禁**永远绿**、它自己开出的 remediation 同样恒绿，两边**从未比过任何字节**（归组重构后一直如此，P-02）。第三个问题在修复中显形：vendor 副本与 `node_modules/<包>` 本是同一个 inode（`links=2`），`applySync` 按红线用 tmp+mv 写 vendor 会**换掉 inode**，`node_modules` 那份留在旧内容 → 「同步成功」与「运行时读得到它」再次分家，而 `loadPointFiles` **刻意把 `package.json` 排除在断言面外**，故这次分家没有任何仪器看得见。决策：①路径一律用**归组后的 repo 相对路径**，不取 basename（取 basename 是把「当前目录布局」当成常量，而布局恰是本仓库自己重构过的东西）；②**「比了 0 个包」必须与「逐字节都一致」在读数上不同形**——CLI 两侧（vendor 与装载点）零命中即 `warn` + 退出码 1，门禁侧断言 `compared.length > 0` 否则判红，且护栏必须覆盖**每一条**分支（原先只挂在装载点分支上，于是它保护的恰好只有自己那条）；③清单改动的生效路径写进包契约（仓库 → `--only-metadata` 写 vendor → 同一 tmp+mv 语义补写装载点 → 重启）；④装载点 `package.json` 按**装载字段子集**（`main`/`exports`/`dsh`）对账——纳入整个文件的前提是「pnpm 会在装载点重写它」，本机实测**该前提不成立**（装载点那份含 `scripts`/`devDependencies`、与仓库逐字节相同），但整文件比对又会在 pnpm 真去剥这两个字段时误红，故取装载字段子集；`files` 不在其中（交付形态已由 `profile-files-sync` 管，同一事实不设两个家）。验证：两台空射程仪器从「比 0 个包恒绿」变成真的比对 **24 个包**，阳性对照（故意拿别的包当源）确认判红；装载字段对账同样过了阳性对照；全量实测漂移恰好只有本包 1 个，故开启真比较无连带影响；三处 `package.json` sha256 一致（`1d048187…`），`lib/client.js` 仍是 `585bbdb2…`（本次只动声明、未动产物，构建前后字节相同）；门禁 56/56。诚实划界：只对账装载字段，不保证 pnpm 在别处（如 workspace 提升、锁文件）的行为；`files` 与产物字节的完整一致性仍由既有两项门禁负责。 | accepted（2026-09-15） | [Note](../notes/implemented/surface/2026-09-15-settings-shell-reachability-and-grouping.md) |
| ADR-0089 | 全栈线 40 条入库：**证据语料必须先于判定**，无岗是一等结论，文档不许承诺没被守住的分型。本轮三条读数否掉了「照既有脚本跑一遍就行」：①归位判据 J2 要求 `from_skill` 是技能原文连续子串，而原文来自 `manifest/skill-evidence-corpus.json`——本批 40 条**一条都不在语料里**（停在 T0 的 251 条），后果不是红灯而是**静默降级**：覆盖不到的证据被记「不可复核」，整批判定变成一句无法回答的话；②40 个技能头像的徽章选型（肤色/发型/衬衫/领口/图形）只活在本机 lute-brand-icons 资产里，零留档；③SOP §12.5 在 09-14 宣布把 `no_role_kind` 扩到 7 类却**只改了文档**，校验器 `NO_ROLE_KINDS`、测试分型集合、manifest 用例一处都没动——照文档判就会撞红，而 `J4-NO-ROLE-KIND` 的措辞把责任指向判定人（本批绝大多数正是「工程能力」那类，两个独立执行者各自踩到并独立上报）。决策：①`scripts/build-evidence-corpus.mjs` 补齐语料，**既有 251 条逐字节不动**（它们抓自另一份源头，用「当前已装技能」重算等于悄悄换掉判据基准），`--check` 供门禁 + 写盘前证明「零改动往返逐字节相同」；②判定与合并分离——四批并行产出**片段**，`scripts/apply-role-fragments.py` 单点合并并**重算** `coverage` 四个派生量与 `_meta.purpose`（手改一次要同改五处，漏一处表现为「门禁报一个和本次改动看不出关系的错」）；③自检器 `check-fragment.py` **直接 import 正式校验器的纯函数**把片段叠加到真表上跑，不复制判据（复制就是「两个读取点口径不一致」这个老形态）；④无岗是一等结论：本批 8 个挂载 / 32 条无岗，理由要点名最近候选岗位并说明为什么对不上、主动否掉近邻；⑤§12.5 表加**状态列**（生效四类 / 🕓 未生效三类）并写明启用需同改哪三处，同时加门禁测试把「文档标为生效的分型」与「校验器实际接受的」逐字对账，且文档举了例子的分型必须真在 manifest 里用过——把「知道」变成「拦住」；⑥新增 `scripts/add-fs-brand-icons.py` 把 40 行 `sk-fs-<name>` 按 id 归入 catalog 既有块，**徽章选型随脚本进仓库**；⑦`verify-fullstack.mjs` 的计数改从映射推导，SOP 的「硬编码 +1 易漏」一行改为记录这次去硬编码。验证：归位表 253 → **293 条**（有岗 225 / 无岗 68 / 挂载 338），J1–J6 六条全过，**技能证据 338/338、岗位证据 338/338 全部可机械复核**（责任名 336/338），复核覆盖 **1012/1014**——未达 100%，两处是「责任名留空但写明理由」的诚实标注，**照实报不得说成全部通过**；全栈目录 8 组 / 70 行、`skill-icons-fs.json` 30 → 70 枚、`verify-fullstack` **70/70**、包内测试 71 → 72 项全绿、仓库门禁 **59/59**。负控：新增的文档↔校验器对账测试三次变异（校验器偷收 DEV_ENGINEERING / 文档把在用分型标成未生效 / 抹掉某分型唯一用例）均如实变红，还原后复绿。诚实划界：SOP 第 4 项运行层验收**未运行**（宿主进程内存持有 catalog，生效需重启 DSH，而重启会中断当时会话），接口读数仍是旧值 30，重启后各组期望值已写进 Note 供直接对账；跑 `assign_lute_icons.py` 顺带把两份**陈旧导出**的 gn 图标清单对齐到当前 catalog（枚数键序不变）；`generator.js` 把衬衫名当颜色传进 `shoulders()` 致**全部 310 枚图标肩带渲染成纯黑**（实测肩带取样 (0,0,0)），修法一行但会让 270 枚已验收图标外观整体改变，未擅自改。 | accepted（2026-09-15） | [Note](../notes/implemented/capability/2026-09-15-fullstack-40-role-assignment-and-icon-pipeline.md) |
| ADR-0090 | 品牌图标的下装颜色：一个**看着像设计**的非法值，与它为什么能活过 310 枚。起因是给新徽章做像素验收时测到肩带是纯黑 `(0,0,0)`，而 catalog 写的是白衬衫 `W`。三件事叠在一起：①生成器把 `catalog.js` 的 `shirt` **名字**（`G`/`GD`/`GL`/`MINT`/`W`）当**颜色值**传进 `shoulders()`——而紧邻的 `COLLARS` 表用的正是 `${W}`/`${G}` 值，两个用法并排写在同一个函数里，读起来毫无违和；产物写着 `fill="W"`，按 CSS 规范非法、SVG 回落到纯黑。②它**长得像设计**（小人像配深色下装很合理，全库一起错反而更像风格统一）——假绿比红灯贵：连红灯都没有，是「看起来对」。③**没有任何判据在看这个值**：「图标覆盖」只问「这一行有没有图」，不问「这张图能不能正确渲染」，而生成器在本机 `~/.dsh/skills/` 下、不进仓库。③背后还有一条更隐蔽的路：`assign_lute_icons.py` 的「保留既有自定义图标」把**上一轮 `skill-icons.json` 的字节**当事实源，三条非 81 系技能正走它——**生成器修好了也照不到它们**。决策：①补 `SHIRT_COLORS` 表并传值；②判据挂在**被消费的产物**上（`verify_static.mjs` 解全部 data URI 断言 paint 合法 + 一条**空转哨兵**，使「解出 N 枚却一个 fill 都没有」与「零违规」不同形），不写进生成器（它不在仓库，门禁够不着；两处都写＝同一事实两个家）；③`EXTRA_ASSIGN` 把三条非 81 系指回 catalog id（84 枚全部现算），沿用路径**保留但点名**。验证：修前 **193 枚不同的图**带非法 paint（被 230 个清单条目引用：出海 84 + 全栈 70 + 通用 15 + 三类分组 49 + 层头像 12，**一枚例外都没有**），修后 193 枚 / 6720 个 paint 值全合法；取样实测 `sk-fs-tdd` `(0,0,0)`→`(255,255,255)`、`sk-fs-accessibility` `(0,0,0)`→`#58B848`；受控变异（把 `fill="#FFFFFF"` 改回 `fill="W"`）当场判红、还原复绿；沿用路径负控打印点名。连带重建 6 份清单 + `lib/layer-icons.js` + **隔壁包 `dsh-algo-skills-local/src/layer-icons.ts`**（两包逐字节相同、有跨包测试守着，只跑一边会当场红）。诚实划界：判据断言的是**paint 值合法**，不是「这枚图看起来对」——图案与配色是否合适是人的判断（与 ADR-0081 同型）；本机生成器不在仓库，仓库能守的只有那两条。  | accepted（2026-09-15） | [Note](../notes/implemented/capability/2026-09-15-brand-icon-shoulder-paint-and-carry-over-path.md) |
| ADR-0091 | 「三无 · Agent全栈专家」preset 与全栈目录的节点轴重建。三件事同时成立才算完成：①**人格**要可执行——正文之家是仓库内 `presets/agent-fullstack/SOUL.md`，`agent.cordis.yml` 的 persona 行是它渲染出来的**物化副本**（`sync-fullstack-persona.mjs`），门禁 import **同一个**渲染函数复算并逐字比对，且不只比字节：三无三条、M00–M13 十四个节点、`{{model}}`/`{{cwd}}` 两个运行时占位符、长度下限、非骨架占位逐条独立断言——理由是人格的腐烂**完全不可见**（加载/日志/页面读数全正常），而「源与副本一起删」这种改法字节比对挡不住。②**技能目录**收窄到 89 条白名单（覆盖 M00–M13，无一空节点）并打开 `respectFileFlags`——它缺省 false，那一刻子集内技能的 `disable-model-invocation`/`user-invocable` 全部失效而一切读数正常（ADR-0083 那条用户状态被静默覆盖），这是本 preset 存在的唯一理由；压缩后端回到**官方** `compaction-basic`（dsh-dcp 私有键会被 `validateKeys` 拒绝并让**整行不加载**）。③**页面与人格同轴**：技能中心全栈线由 8 个「做法类」分组改为 **M00–M13 十四个交付节点**（8 组 / 70 行 → 14 组 / 138 行），节点键与标题的唯一事实源是 `build-fullstack-catalog.mjs` 的 `NODES`，派生 mapping.categories / manifest / taxonomy.fullstackNames 三份产物，`verify_static` 的新判据**不写任何数字**、一律与事实源对账；顺带补上一条此前**根本不存在**的判据——全栈线分组头像必须存在且**互不相同**（`build_preset_catalog.py` 缺键时回落到出海线的 `agent-tools` 头像，那不是空值，所以「这行有没有图」看不见它，14 个节点会一起顶同一张脸）。④**`lute-cordis` 保留目录、只标退役**：引用面实测 **329 个会话**（302 subagent + 27 顶层，顶层里 16 个有人类消息 / 1709 条用户消息 / 400MB）指向它，`remove-preset.mjs` 拒绝执行是对的；平台 schema 只有 name/description/order/icon，**没有 hidden**，所以「藏起来但仍可解析」不存在。负控：人格层 11 条突变自测（含**同长度**单字符替换，打掉只比长度的退化实现；源与副本**一起**删 M09 后同源仍成立而断点仍红）、对真 preset 注入突变→红→同步器拉回后 sha256 逐字节复原、README 组数 14→15 判红、新 FS 判据改造前真实红过四条。验证：`verify_static` 绿（FS 14 组 / 223+138 行 / 图标 199 枚 6911 个 paint 全合法）、包内测试 72/72、门禁 quick **63/63**。诚实划界：**页面生效需重启 DSH**（宿主在插件装载时 import catalog，本会话内改文件不热更新），运行层验收**未做**；14 枚新头像只过结构判据（品牌 9 条不变量 + 徽章确实画上 + 与重算逐字节相同），**未做像素级/双主题预览验收**（本会话模型不能读图、无截图工具），`preview-*.html` 仍是 310 枚旧版；68 条新技能无专属头像、回落所属节点分组头像（脚本点名打印不静默）；本机品牌清单 310→324 枚不在 git 下且**没有跑 build.js**（它会整体重写清单，有重算已验收字节的风险）。 | accepted（2026-09-16） | [Note](../notes/implemented/capability/2026-09-16-agent-fullstack-preset-and-node-axis.md) |
| ADR-0092 | 「三无」的头像：**形制承载等级，颜色不承载**。起因是 53 个 preset 里 `agent-fullstack` 是唯一还没有 `icon:` 行的现役 preset（`icon` 是官方卡片显式消费的字段，缺了静默回落成「没有脸」），而品牌铁律「不引入品牌绿以外的彩色」**排除了金色**——皇帝冕冠最省事的画法正好违规。决策：①**不用金色**，帝王感由形制（冕旒 / 梁冠 / 交领右衽）与灰白须眉表达，颜色只用品牌绿三阶 + 白 + 灰；②**引擎只追加 key**，动笔前用漂移闸证明既有 **324 枚重建后逐字节相同、零例外**（那些字节被别的包断言着）；③走 **append-only 通道、不跑 `build.js`**（它会整体重建 manifest 并重算全部已验收字节），新脚本多一道**部件指纹闸**——每个新部件登记「画出来必然留下的子串」，防的是「key 名写错时 `buildIcon` 不抛错、静默少画一块」这个既有缺陷形状；④**同源契约**：图标库 manifest 是家，`preset.yml` 的 `icon:` 行是**物化副本**，`sync-fullstack-avatar.mjs` 是唯一渲染实现、门禁 import 同一份函数复算，第 7 层分三问（形状 / 同源 / **paint 合法性**），图标库读不到时**判红并给修法**而不是静默降级（空转的判据仍然报绿是本仓库登记过的形态）；⑤**判据挂在被消费的产物上**——这枚头像的载体就是 `preset.yml`，仓库够得着，所以 paint 合法性由仓库自己读（ADR-0090 的纪律延伸到新载体），而图标库自己的结构/栅格判据不在仓库、按同一条界算**本机工具**。负控：自测 12→**19 条**（I0–I7）：删 icon 行、写成相对路径、data URI 解出来不是徽章、停在上一个版本、图标库读不到、非法 paint 值（ADR-0090 的形状）、零 paint 空转哨兵，逐条判红；干净副本静默。验证：漂移闸 324/324 逐字节相同；结构判据 327 枚 4227 项全绿；栅格判据 **0 像素越框**（先在已验收图标上校准）；`gate` **63/63**、`gate:full` **70/70**。诚实划界：**模型不能读图**，审美判断未做也做不了——交付的是三枚候选 + 对照图（用户选定冕旒），好不好看是人的判断；**判据自身翻车四次**（全部 324 枚被第一版判红、把渐变定义当越界、把 `stroke-width` 当坐标、栅格判据把不透明背景当内容），四次都靠「先拿已验收产物校准判据」挡住；`headset` 杆尖越界 24 枚为**既有**缺陷，登记为具名豁免且只减不增；`agent-fullstack` **未登记**进 `packaging/shipped-presets.json`，实测 `select-presets.mjs` 会拒绝打包并点名它——发不发是产品决策，未擅自表态；卡面头像需**重启 DSH** 才可见。 | accepted（2026-09-16） | [Note](../notes/implemented/capability/2026-09-16-sanwu-avatar-and-icon-library-same-origin.md) |

| ADR-0093 | preset/skill 破坏性入口统一采用 **validated direct child + journaled directory transaction**：final name、canonical root、symlink/hard-link、dev/ino 与逐树 SHA-256 在 live rename 前证明；锁后 staging/fsync，逐项 `old → backup/quarantine`、`stage → target`，失败逆序 rollback。preset 先完整 archive 再 rename 到 retained quarantine，不做立即删除；skill 以完整目录 swap，保留 resources 与旧版本。live mutation 必须显式 `--apply`；硬退出后的 stale lock 只有 journal token/batch 相等且 owner PID 已不存在时，才允许 `--adopt-orphan-lock` 明确接管。多目录只承诺 journaled crash consistency，不冒充单点原子；第三方 apply 在 SEC-RT-002 审批账本完成前保持 fail-closed。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-preset-skill-journaled-transactions.md) |
| ADR-0094 | gate 结论统一为带分母的 canonical `pass/fail/skip`：守恒、空射程、throw、文本/JSON 与 strict no-skip 共用一份结果；旧 checker 只经集中 adapter 迁移。`live-presets` 采用受控 composition row parser，使裸包与六类 specifier 全进分母、未知 dynamic fail-closed，并以不含用户配置正文的 per-preset row identity SHA-256 inventory 固定结构；删、增、替换一条仍可解析的 row 都必须非零。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-gate-result-accounting-and-live-preset-inventory.md) |
| ADR-0095 | third-party intake 以版本化 upstream source ID inventory 闭合；`imported` / `skipped` / `alreadyInstalled` 三终态互斥，duplicate、overlap、missing、unexpected 与同总数替换均逐 ID 判红。所有问题必须在成功输出/写入前收口，`--check` 只读，生成仅以同目录临时文件 + fsync + rename 原子替换；分类 inventory 不冒充不可变 provenance。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-third-party-intake-accounting.md) |
| ADR-0096 | Fullstack 的 mapping 70 与 extra 68 先合成带来源的 canonical catalog，再以同一逐项契约核对 138 条；root gate 独立保留 catalog 138 与 whitelist 89 两个分母。owner `lute` 明确确认 exact 89 集合及 `4ebfa9f…` 指纹后才物化 tracked manifest，live 与批准集合双向全等；scope 仅为 preset composition，不冒充 invocation policy 或发布授权。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-fullstack-catalog-and-approved-whitelist-boundary.md) |
| ADR-0097 | Mutation test 只在自有临时根提交副作用：唯一 `repo/home/profile/tmp`、prepare→commit、路径 containment 与 dev/inode ownership cleanup；根包、Settings、worktable、fullstack 四类真实污染点和六组无 cleanup fixture 已迁移。现存 suite 迁移归 QG-006A，完整并发/SIGTERM/worktree snapshot 仍归 QG-006B。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-mutation-fixture-isolation.md) |
| ADR-0098 | Settings Shell 只消费 parser-owned root marker；关闭、替换、漂移与失败时回收。AX 以 pinned upstream 的 188px nav 与 28×28px close 两个节点独立校准，目标按钮不得参与 scale；缺锚、冲突、错窗、权限与 timeout typed unavailable，离线证据不冒充 live。 | accepted（2026-09-16） | [Note](../notes/implemented/surface/2026-09-16-settings-owned-scope-and-independent-ax-calibration.md) |
| ADR-0099 | Wanzh 状态持久化收成插件内唯一原子写入器（同目录临时文件 + fsync + rename 前校验权限位 + 目录 fsync），并把「文件不存在」与「内容损坏」分成两种读数：损坏时保留原字节取证、能力 fail-closed、不拿默认清单替代。OAuth 流程收进单一所有者（supersede、到期定时器、幂等 close、卸载后拒绝新建），路由注册与回收由同一份清单驱动。 | accepted（2026-09-16） | [Note](../notes/implemented/capability/2026-09-16-wanzh-persistence-and-oauth-lifecycle.md) |
| ADR-0100 | 两处 HTTP 面（wanzh 插件、team-hub 网关）的请求体读取收成同一份有界契约：声明或累计超限 413、超 deadline 408、中断 400、解析失败 400，错误一律不回显原文，只信实际字节而不信 `Content-Length`。上限按「面」分档，透传档由实测天花板（上游 `dsh-file-upload` 的 18 MiB）推导。因两个包都无构建步、无法消费 TypeScript 共享源，契约由两份实现承载、由两侧同一组用例守住。另定：`try` 块内返回 promise 必须 `await`，否则拒绝不被 `catch` 接住。 | accepted（2026-09-16） | [Note](../notes/implemented/capability/2026-09-16-bounded-request-bodies.md) |
| ADR-0101 | 交付白名单（`files`）的完整性判据从「装载点侧」补到「交付清单侧」：新增门禁 `package-files-coverage`，断言包里**运行时模块图**（声明入口的可达闭包，含字面量 `import.meta.url` 引用）上的每个文件都在 `files` 射程内。立论基础经实测钉住——`pnpm` 对 `file:` 依赖按 `files` 物化，缺件命中的是全新安装而非仅发布面。判据面刻意**不取**「`lib/` 顶层所有 bundle」（那是装载点判据的口径，搬过来就是假红）；判定器与真实 `npm pack` 对全部受管包逐文件校准；无 `files` 白名单的包按 ignore 文件能否命中运行时文件分流成读数或类型化 skip，不写成 skipped 以免把 `gate:strict` 永久拖红。 | accepted（2026-09-16） | [Note](../notes/implemented/contract/2026-09-16-package-delivery-allowlist-coverage.md) |
| ADR-0102 | 门禁的分母必须由「被检查对象的完整集合」推出，而不是由「恰好读到的东西」推出。QG-003 / QG-004 / QG-005 三张卡各有一个实测 Red，形状不同、根因同一个：**射程变小与「没有问题」在读数上同形**（`main...HEAD` 自比较恒为空；候选读不到就 `continue` 从 23 掉到 21 仍绿；profile 清单解析失败被吞成「没有 file: 依赖」使三个门禁一起静默变绿）。三处统一改成对象级账目 `expected = checked + skipped + failed`：射程按 merge-base + 四类工作树来源求并集且基线不可解析判红（不含本地 `main`）；插件入口按清单解析并跟随转出口、四类候选全进分母；profile 期望集按完整相对路径从声明推出且只允许「根整体不存在」一种 skip。 | accepted（2026-09-17） | [Note](../notes/implemented/contract/2026-09-17-gate-scope-is-the-denominator.md) |
| ADR-0103 | 门禁的「无副作用」必须由**快照见证 + 进程级回收**证明，而不是由 `finally` 声明。QG-006B 实测两个 Red：`kill -TERM` 一个运行中的门禁留下 8 个临时根并把一个门禁子进程交给 init 收养；再数真实 `TMPDIR` 得 **2,066** 个历史残留根、35 MB（三个来源：从不清理的 `fixture()`、只建不清的 `makeTree()`、只在正常跑完时执行的 `test.after`）。新增 `repo-snapshot.mjs`（纯函数快照：git tracked/untracked 内容哈希 + 声明根深度 1 名录 + 未声明 ignored 区域全量 + HEAD/refs/index 字节；空射程、目录折叠、枚举与磁盘不一致一律判红）与 `repo-attest.mjs`（真子进程、独立进程组、before/after；差异与「说不清」分开报告），六条结束路径逐条见证；`mutation-fixture.mjs` 加进程级回收（live 根注册表 + `exit` + 信号先回收再重发默认处置）与窄接口 `mutationRoot()`；`gate --attest` 把同一契约对 CI 公开。 | accepted（2026-09-17） | [Note](../notes/implemented/contract/2026-09-17-self-attestation-and-process-reaping.md) |
| ADR-0104 | 第一阶段借用 Codex Desktop 的空间与视觉语法，保持 LUTE/DSH 品牌、业务色、信息架构和 vendor 边界不变；以 `dsh-theme-local` 作为浅色/深色/系统三态的唯一 token owner，默认采用中性浅灰绿/深绿灰画布与可访问的 LUTE 绿色，并按 Settings → 抽屉/Composer → 业务页面渐进迁移。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-codex-default-visual-system.md) |
| ADR-0105 | CI 的判据面必须能被**离线证伪**：新增 `.github/workflows/gate.yml`（`quick` on PR+push、`full` push-only，各带 timeout / 门禁 / `--attest` 见证 / `if: always()` 证据上传 / 只读 permissions / 并发取消 / 钉住的 Node+pnpm）与离线判据 `ci-workflow-contract`（含一个只认窄 YAML 子集的读取器，读不懂即判红）。本卡执行时 **GitHub 不可达、容器 daemon 未运行，L2/L3 未取得**，因此判据固定回报 `authority: 'L1-static'` 并在读数里写「静态审计，不等于 CI 已验证」；反向自测 27 条（21 条具体事故变异）。另用**干净 HOME 干跑**把「干净 runner 上哪些项没有射程」变成读数——它当场抓出 `skill-lines` 自测 S2/S3 读真实 `homedir()`（S2–S7 现全部注入合成 HOME），并顺带修掉聚合层「混用 canonical/legacy 字段只报 schema invalid」的报错。 | accepted（2026-09-17） | [Note](../notes/implemented/contract/2026-09-17-ci-workflow-contract-and-clean-runner-simulation.md) |
| ADR-0106 | `main` 与 `v*` tag 的保护**由机制守着**，而不是靠文档声称：声明 `scripts/gates/ruleset-declaration.json` 是唯一事实之家；只读审计 `audit-rulesets.mjs` 逐条比对 API 读数，退出码 0/1/2 = 全等 / 不符 / **拿不到读数**（卡面负例：权限不足不得解释成「未配置所以通过」）；payload 由声明推导（`apply-rulesets.mjs`，默认 dry-run），反向自测 15 条的基线取自**真实 API 读数**。实施中修掉两处：判据误读列表端点（缺 `conditions`/`rules`，会把「保护在」判成「保护不在」），以及 `node-interpreter` 判据当场抓住新代码里裸用 `process.execPath`。同轮更正了 [ADR-0001](ADR-0001.md) 「main 分支保护」这处长期失实的声明。 | accepted（2026-09-17） | [Note](../notes/implemented/contract/2026-09-17-branch-and-tag-protection.md) |
| ADR-0107 | 管理与研究型页面统一采用「基础画布 → 内容面板 → 顶层 modal」语法；只改 Team Hub admin UI、Agent Team Recipes、Deep Research 的视觉表现，保留业务数据、权限、RPC、状态机、路由和 slot 边界。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-management-research-surface-visual-contract.md) |
| ADR-0108 | New App、Role Matrix、Skill Center 在不改变 top-layer、业务状态、路由、slot、权限和 vendor 的前提下，共享抽屉控件、状态、焦点、动效和窄屏视觉合同。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-drawer-control-contract.md) |
| ADR-0109 | Settings 内容列采用单一滚动区与窄屏重排；Skill Center、Deep Research 的浮层改用低于 Settings root 的语义层级变量；不修改 vendor、业务按钮语义或 compiled-only 产物。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-settings-content-and-layer-contract.md) |
| ADR-0110 | 收口 Recipes、Deep Research、Team Hub Admin 的视觉多源问题；Wanzh hand-written drawer 使用低于 Settings 的语义层级；Task Board、My Quotes、LoopX、UI Polish 继续按 compiled-only artifact 边界管理。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-surface-visual-closeout-and-artifact-boundary.md) |
| ADR-0111 | 打包链的基线版本串从「写死一代（2.0.5）」改为从 staged app 的 ShortVersionString 派生（CFBundleVersion 戳 / 向导版本 / VERSION 凭据 / manifest dsd_baseline 四处联动 + 新增 DSH_RUNTIME）；install.sh 预写向导状态改读载荷凭据、缺字段跳过不编造；smoke 的 app 侧 node_modules 改走 app-resources 布局探测（第 5 个消费面）。T-00 备份树 7.9G 迁出仓库工作树——untracked 全量读取曾把 repo-attest 快照拖到 174.8s（selftest 120s 必超）。 | accepted（2026-09-17） | [Note](../notes/implemented/packaging/2026-09-17-version-credentials-single-source-and-backup-relocation.md) |
| ADR-0112 | 出货技能面不携带 `__pycache__` 字节码：`.pyc` 的 co_filename 钉死构建机绝对路径，是 ADR-0056 文本面守卫读不到的二进制载体（2.4.1 起带出 18 个，r4 深检发现）。select-skills 的 --copy 加 cpSync filter 净化 + --check 新增判罚判据，双机制缺一不可；vendor 面 stale pyc 留尾债族。 | accepted（2026-09-17） | [Note](../notes/implemented/packaging/2026-09-17-skill-surface-pycache-exclusion.md) |
| ADR-0113 | 供应链不可变性固化（SEC-RT-002）：MCP 配置锁定确切版本、去除 @latest；LoopX 锁定精确版本 `loopx==0.5.4`、去除 --upgrade 且加 --no-deps；第三方技能拉取绑定 40 位 hex commit SHA、消除 trees/HEAD 与 /HEAD/、启用 SHA-256 + Git blob OID 双重比对、剔除 /tmp 回退；新增 `immutable-supply-chain` 门禁与自测强制保障不可变性。 | accepted（2026-09-17） | [Note](../notes/implemented/security/2026-09-17-sec-rt-002-immutable-supply-chain.md) |
| ADR-0114 | 补丁里的环境常量必须随基座形态一起被判定：P0-8 把 pi-ai 懒加载路径写死成 `app.asar.unpacked`，基座迁到 no-ASAR 后悬空（2.5.0 装完即恢复模式），而锚门只查「常量名在不在文件里」所以全绿——P0-8 整体退役、静态 import 回归、解析交还 Node；LUTE 写进 v0 会话的 `skill-catalog.title` 被迁移器精确成员校验拒绝（470 份会话 74→469 可迁移），改为准入可选 `title` 并保留类型校验；新增 `resource-path-reachability` 门禁钉「路径在产物上真实可达」（含仪器假红防线）。 | accepted（2026-09-17） | [Note](../notes/implemented/packaging/2026-09-17-resource-path-reachability-and-v0-title-admission.md) |
| ADR-0115 | 浅色主题取真中性骨架：codex 预设浅色六值去彩度、品牌绿只作强调（深色同步去彩度，深浅是同一套灰阶的两种曝光）；官方前端真实消费的 44 个 alias 从只接管 19 个扩到全量接管（新增 25 个；官方不消费的 35 个不接管）；字面兜底上收为构建时从 presets.ts 生成的静态默认表，surface 不再自带色值；浅色 layer-1/2/3 修正为与深色同向的单调递增；新增 `theme-alias-coverage` 门禁钉「官方消费 ⊆ 本仓接管」；已落仓 `theme-palette-audit` 审计脚本（五组取证 + 真实引擎求值，带 16 条仪器自检与 `--break` 突变验证），本 ADR 全部数字由它产出、可逐条复跑。 | accepted（2026-09-17） | [Note](../notes/implemented/surface/2026-09-17-light-theme-neutral-palette-contract.md) |
| ADR-0116 | 预设行的 config 对着**插件自己的 schema** 判，工具不得把上游的键名钉成字面量：2.0.10 把 `@deepseek-ai/dsh-persona` 的键从 `text` 改成必填 `prefix`，产物面（2.5.0 出货载荷 52 行）、同步器锚点正则、两个 legacy 生成器同时钉着旧值而无一判红；新增 `preset-config-schema` 配置镜（三层判据 + 四态分母 `discovered = ok + failed + unverifiable + notApplicable`，射程含待发布出货载荷，接线到 `assemble.sh` 的 tar 之前）与 `preset-config-schema-selftest`；同步器锚点改为认结构不认键名（键名任意、写回原样保留、0 或 ≥2 个块标量响亮失败）；关键负结果：键名**无法**从 `Config.dict` 推导（四键全 `required:true`，无区分度）。 | accepted（2026-09-17） | [Note](../notes/implemented/contract/2026-09-17-preset-config-schema-mirror-and-structural-anchor.md) |
| ADR-0117 | 实况探针（C4）的「取不到值」分两类：**全局根声明断链**判红、**局部作用域**不判红——探针挂在 body，局部类上的 token 本就不在继承链上，旧版把它记成「层叠失效的指纹」属假红（实测 `--dsw-hovercard-bg` 声明于 CSS Modules 类 `._card_1b2ny_13`、页面无元素挂载）；声明收集由 `Set` 改为 `Map<name, Set<selector>>` 并补上容器规则（`@media`/`@supports`/`@layer`/CSS 嵌套）递归，修掉反向的**静默漏测**（补后抓到此前完全不可见的 `--dsw-corner-shape`）；退出码收紧为「仅全局未解析或跨域样式表读不到才判 1」，`unresolved` 字段保留全部语义、新增 `unresolvedGlobal`/`unresolvedLocalScoped` 与逐条 `declaredIn` 溯源；实测浅深双态各一次（用户手动切换主题），两态均全局未解析 0、局部 1、仪器自检 6/6、exit 0。 | accepted（2026-09-18） | [Note](../notes/implemented/contract/2026-09-18-theme-live-gui-unresolved-classification.md) |
| ADR-0118 | profile 插件对官方 UI 的依赖必须成为**可被判据读取的声明**：2.0.10 把 hero 的独立文本元素去掉（`headlineText` 消失，标题变成 `titleGroup` 里一个无类名的 span），插件的隐藏规则因此根本没生成（活应用 `data-dsh-root-brand-anchors = degraded:heroHeadlineText,statsLineRoot`），而门禁零 acceptance 接线、发布 SOP 无实况验收、品牌探针在新基座上第一步就 exit 3、插件的真实产物接缝 5 条用例整段 `skipIf` —— **「知道」没有变成「拦住」**。修法：依赖写进包根 `ui-anchors.json` 作为唯一家（插件 import 它、门禁读它），`ANCHOR_KEYS` 与清单 id 双向断言；标题改为按**关系**定位（角标父容器里唯一的有文字叶子兄弟）而非类名，找不到时不猜（0 或 ≥2 候选只报 degraded，不隐藏新的东西，已识别的保持隐藏）；退役已被上游换掉的统计条折叠；新增阻塞门禁 `plugin-ui-anchor-drift`（声明 × 产物，射程 = 装机 app ∪ 未打 tag staging，两种红必须可区分，空射程报 skip）+ 反向自测；插件测试接缝改硬前置并接回 `scripts/lib/app-resources.mjs`；实况验收进 SOP §0；staging 构建树退役（5.1G → 164K，`.freeze-*` 指纹保留）。实测：门禁 87/90 退出码 0、插件测试 22 passed/0 skipped（原 9/5）、实况探针 21/21。 | accepted（2026-09-18） | [Note](../notes/implemented/contract/2026-09-18-plugin-ui-anchor-mirror-and-hero-title-relation.md) |
| ADR-0119 | 第三方技能入库必须**走机制而不是走笔**：登记由 `intake-install.mjs` 产生，不由人照形状填写——该文件的 `repairs`（丢/改/重生成了几份）与 `lintWarnings` 是安装器**量**出来的读数，人只能**断言**，而手写还会制造这份事实源的第二个写入者（P-02「未验证的事实被钉进出货面」）。`kami`（纸感排版系统，`github.com/tw93/Kami` 的 `skills/kami/`，MIT，v1.15.0 @ `6b39d04`）在本机先以**手装**形态存在：文件齐全、字体已下、断网渲染通过，但对三处事实源是隐形的——`manifest/intake-provenance.json`（无逐文件 sha256 / 许可证结论 / 已应用修补，上游升级与回滚失去比对基准）、`manifest/runtime-deps.json`（`gate:skill-runtime-preconditions` 的射程是「清单里有声明 ∩ 已接线」，清单里没有就**静默**不查，读数只显示总数小一而不显示漏了一条，P-11）、以及各 `agt-*` 的 `skill-subset`（对 50 个岗位会话等于不存在）。决策：①登记必须由 `scripts/intake-install.mjs` 产生，本地偏离只走管线既有的两条通道（正文 → `staging/translations/<name>.body.md`；文件级修补 → `staging/intake-repairs.json` 的 `dropFiles`/`writeFiles`/`rewritePaths`，能用恒等替换表达时不用 `writeFiles` 冻结整份文件）；②新增来件批次在 `scripts/intake-lib.mjs` 的 `BATCHES` 登记，`source` 必须回答「从哪个仓库来的」（批次名会流到 frontmatter 的 `metadata.batch`、provenance 的 `batch` 与设置页来源标签，不许写 `github` 这种无信息量的值）；③**重跑安装或扫描之后必须重新占位化机器路径才允许提交**——两者的写入者都用 `FROM`/`VENV_PY` 重写两份 manifest 的 `_meta`，而入库形态是发布期 `rewrite-build-paths.mjs` 处理过的 `__SKILL_INTAKE_SOURCE__`/`__DSH_HOME__`，实测 `_meta.from` 会从占位符变回 `/Users/lute/…/Downloads/skills`（ADR-0056 机器路径只减不增），提交前自查 `grep -c "$HOME" <两份 manifest>` 期望 0；④**判据的射程缺口要写清楚，不许并进「已覆盖」**：`runtime-deps.json` 的 schema 只有 `python`/`cli`/`node` 三类，Kami 的 4 个中文字体（不在技能单元里，由 `scripts/ensure-fonts.sh` 下到用户字体目录）表达不进去，而缺字体的症状正是 P-02 最隐蔽的形态（退出码 0、PDF 也生成了，坏在交付物里）——本版把它写进 SOP §12.13 与 provenance 的 `knownGaps`，并明确**没有门禁在读**。验证：走管线实装 111 文件、lint 警告 0、`[修补 丢0/改1/生0]`；逐文件比对上游只有 `SKILL.md` 与 `scripts/verify.py` 变，且两者与手装版**逐字节相同**（正文覆盖 44,381 B、`rewritePaths` 一条恒等替换）；断网复验 `--check-fonts` 仍是 TsangerJinKai02 / 359 表意字；门禁射程由 296/5 变 297/6，`--check --subset kami` red 0；接线 50 个 preset 各 +1、移除 0、新增并集恰为 `{kami}`、总条目 1189 → 1239；`verify-generic` 16/16 且接线前会逐岗点名判红；L1–L12 断言 5106 → 5156。附带修掉一个沉睡缺陷：`promote-intake-batch.mjs` 的 `WRITERS` 少写尾随换行，致其写盘前自检**对任何输入都失败**（自检挡住的是它自己），自那两份 manifest 成形起没成功跑过——「一个没有调用方的工具，它的自检坏了也没人会发现」 | accepted（2026-09-18） | [Note](../notes/implemented/capability/2026-09-18-kami-intake-registration-and-preset-wiring.md) |
| ADR-0120 | 安全运行时契约主门禁与自动化端到端验收闭环（SEC-RT-010）：在 `scripts/gates/security-contract-registry.mjs` 中集中登记 SEC-RT-001 ~ 009 及 003A 共 10 项安全契约单一事实源，每项严格声明 targetFiles、testFiles、faultPoints 与 `releaseBlocker: true`；新增 `security-contract-master` 主门禁以硬编码的 10 项契约作为基准分母，任何缺失、目标文件或测试文件不可达、无故障注入点或试图关闭 releaseBlocker 均硬判红；提供 `security-contract-master-selftest` 验证 6 项反向突变拦截能力；新增 `scripts/acceptance/security-runtime-acceptance.mjs` 端到端验收工具，脱机串行执行全量契约静态审计与 23 个测试套件，核验 36 个故障注入点；在 `scripts/gate.mjs` 中注册为主门禁。 | accepted（2026-09-18） | [Note](../notes/implemented/security/2026-09-18-sec-rt-010-security-contract-master-gate.md) |
| ADR-0121 | 对象库不得承载凭据与超大对象（SEC-RT-011）：实测 `.git` 3.9 GB 里一个 2.6 GiB 的 pack **只有 1 个对象**——6,800,745,472 B 的 `packaging/backup/pre-2.0.10-migration/dsh-home-snapshot.tar`，内含与磁盘上当时在用那份**逐字节相同**的 `.dsh/.credentials.yaml`（`77d27c62…`），即**活凭证躺在对象库里**；它只从 4 条 `refs/codex/turn-diffs/checkpoints/**` 可达（codex 对 index 做 `write-tree` 的裸 tree，故 `git log --find-object` 找不到）；另有 183 MB Mach-O 与两个 `tmp_pack_*`（717 MB，`git gc` 因 2 周 `pruneExpire` 删不掉）。三处静默导致它进得去且出不来：`.gitignore` 未覆盖 `packaging/backup/`、codex 的 write-tree 无尺寸/密级筛查、**无任何门禁看对象体积**。决策：新增 `object-store-hygiene`（SEC-RT-011）三条判据（单对象 ≤ 50 MiB、总量 ≤ 1 GiB、垃圾包 = 0，判据面常显分母，空射程判红），阈值以真实读数定（26.66 MB 的 cpython tgz 必须放行、6.8 GB tar 与 183 MB Mach-O 必须判红）；收窄 `.gitignore` 排除 `packaging/backup/` 下体积型二进制（不整目录排除，避免 tracked+ignored 漂移）；修掉主门禁分母钉死在必需项数导致**新增契约自身判 schema invalid** 的缺陷；处置次序为「先救唯一无盘外副本的 `check-fragment.py`（4156 B，只在对象库）→ 删 ref → **定点移除**该 pack 而非 `gc --prune=now`（后者会与并发 `git add` 抢锁）」。实测 `.git` 3.9 G → 80 M，`git cat-file -e ddab355b…` 失败，fsck 退出码 0，HEAD/分支/14 tag/8 远端 ref 未变；凭证轮换判定为不需要（依据属推论，已如实登记）；未联网故「是否曾推远端」为 Unknown。 | accepted（2026-09-18） | [Note](../notes/implemented/security/2026-09-18-object-store-hygiene-gate.md) |
| ADR-0122 | 决策记录面向 AGENT：机器可读决策账本（用户裁决「adr 机制需要修复，每一轮决策的结果面向 AGENT 都要做 adr 记录」）。实测：121 篇 ADR 编号连续、索引一致，**形式完全合规**，但 **121/121 全 `accepted`**（状态字段不承载信息）且 **0 篇**有结构化决策面——留痕把人照顾好了、把 agent 落下了，而本仓库的写入者绝大多数是 agent。决策：新增派生账本 `docs/adr/decisions.json`（源永远是 ADR 文件，由 `node scripts/gates/adr-agent-records.mjs --write` 生成）；新 ADR 必须带 `## 机器可读决策` 的 json 块（`decisions` 非空、每项含非空 `id`/`text`、可选 `constraints`）；`legacyWithoutDecisions` 历史豁免**只减不增**（基线 `git show HEAD:`，与 `exemptions-frozen` 同法），补块后自动移出；新增门禁 `adr-agent-records` + 反向自测。**不给 121 篇历史 ADR 反向补块**——那是把「当时决定了什么」改写成「今天的模型认为当时决定了什么」，属伪造留痕。本项是本仓库第一条**读 ADR 正文**的判据（此前 `adr-index` 只看文件名与索引、`adr-note-links` 只看链接可达）。 | accepted（2026-09-18） | [Note](../notes/implemented/process/2026-09-18-agent-readable-adr-ledger.md) |
| ADR-0123 | 2.5.0 收口次序、清场射程与并发读数纪律。本轮 MECE 深查把 `packaging/release/` 的 9 个历史版本 DMG（约 5.7 GB）归为「无用垃圾 J1」并提出「release 7.2 G 迁出」——**该归类被自己的取证推翻**：9/9 产物带 `uchg`、`release-verify.sh` 对「版本目录在、dmg 不在」判 FAIL、ADR-0067 决策段写着用户裁决「不允许删除发布的版本和历史发布的版本」；两份副本是设计出来的（仓库侧是构建面与门禁的对照物，仓库外是重新克隆也碰不到的不变量仓库），ADR-0067 明确否决过「只做归档不做仓库内副本」。决策：①已发布产物一律不在清场射程内；②清场射程三分（受保护／可删／**需先证明**——体积型二进制与任何「只在对象库存在」的路径，删前必须逐字节证明盘外有同一副本）；③2.5.0 收口次序为「先清债、再定版本号」（清债会改动 `packaging/` 与出货面，先定号等于定两次；重打同号另违反 ADR-0057 决策 6）；④收口作业等并发写入停止，判据是三项读数而非感觉（`git status` 条目数、`.git/index` mtime、项目会话日志 mtime 两分钟不变）；⑤门禁终值只在无并发写入时取，并发假红**标注但不计绿**，也不得因它是假的就记成通过；⑥「无用垃圾」结论必须先过一遍「它被谁承诺过」——出现在 `release/*.sha256`、`uchg` 标志、门禁射程或 ADR 决策里的路径是**证据面**不是垃圾。 | accepted（2026-09-18） | [Note](../notes/implemented/process/2026-09-18-agent-readable-adr-ledger.md) |
| ADR-0124 | 启动带去按钮化：官方「新建会话」与自建「新应用」是两条同轴导航行，不是两个按钮；行轴判据的射程扩到「被插件改写的官方行」，`split` 列退役。用户报的两个现象（「统一调小」「颜色也不搭」）取证后各有物理成因，**都不是尺寸问题**：①**视觉权重倒挂**——官方按钮 `height:38px` + `--dsw-alias-button-elevated-fill` 抬升底色 + `.5px` 边框 + `14px/500` 字重 + 居中，而同一列的会话行/工作区行都是 30–36px 无壳文本行，即全列**用得最少**的两个启动动作扛着**最重**的形态；②**颜色是双轨的**——官方按钮走 neutral 系（elevated-fill + border-l3），自建「新应用」行走 `--lute-brand` 品牌绿实底，同一列两套色系各管一段且无一处规定归属。参照面实测四款同类产品（Codex 随 ChatGPT.app v153.0.8010.36 分发、Qoder CN、Minimax Design、Accio）：**启动位一个按钮都没有**，全是 chrome-free 文本行（行距 30–38pt），侧边栏宽**实测 252–280pt、DSH 279pt**（原文 175–300pt / 350pt 系截图文范围估算，2026-09-18 像素实测证伪）。决策：①两行同走 `nav-band` 轴（border-box + width 100% + margin-inline 0 + padding-inline 10px），官方按钮去外壳（无边框/透明底/左对齐/8px 圆角/36px/13px/24px 图标盒）；②**几何不再内联计算**——删 `applySplitGeometry()` 与 `SPLIT_COLLAPSED_LIMIT`，核心只写/撤 `data-lute-navrow` 属性标记（锚是属性不是类名哈希，ADR-0019），几何归被标记的那条规则；③收起态（<60px）撤标记、让官方自己的 rail 规则接管（36×36）；④行高取 **36px 而非规格草案的 30pt**，因同列既有两行与 ADR-0079 行轴都是 36px，改 30pt 须三行同改、另立决策；⑤判据射程新增 `nav-band` 列与 **`RESTYLED_SHELL_ROWS`**——**被插件改写的官方行**首次进断言面（这是本条最可复用的部分：此前改写官方行等于在判据射程之外动刀），并修掉 `ruleBody()` 把分组选择器续行（`.root,\n.entry {`）误读为 `.entry` 声明块的真实缺陷；⑥品牌色只留给徽标与 3px 活动指示条。验证：门禁自测 18/18；真实 Chrome 布局探针 **33/33**（官方按钮 border=none/bg=rgba(0,0,0,0)/radius=8px、两行同 36px/同宽 256px/同左缘 12px/标签同偏 42px、启动带 40+40=80px、工作区下移 34px 由 flex:1 的 region 等高吸收 574→540）；真实 React 18.3.1 探针 **31/31**；newapp 包 vitest 173/173。**取证中同时修掉三个「把环境当常量」的仪器缺陷**：探针硬编码 `app.asar.unpacked` 包布局（实际已变 `Resources/app/node_modules`）、样式表提取正则钉死 `x-` 哈希前缀（2.0 已去掉）、夹具未冻结 `transition` 致读到中间态（P-02 形态：判据与实现都对，读数在时间维度错）。诚实划界：改写依赖 `newSession` 类名存活，基座改名会让它静默失效到「看起来还行」；生效仍需重启应用（ADR-0078），真实应用内浅/深色截图**均未做**；侧边栏宽度 350→280/290/300pt 的裁决因实测证伪而**作废**；**本条只覆盖 P0 启动带，侧栏四带结构未经批准** | accepted（2026-09-18） | [Note](../notes/implemented/surface/2026-09-18-sidebar-launch-band-navrows.md) |
| ADR-0125 | 侧栏四带契约：启动带两行同轴 36px 堆叠为默认、「工作台 ▸」折叠组收编能力带、内容带增置顶与搜索、系统带冻结；侧栏宽度目标 264px。共识收口的三处结构性缺陷逐一取证：①研究规格的 D1–D5 与 ADR-0124 的 D1–D6**同名异指**（「D5」在两边指两件不同的事）；②记录强度≠共识强度——ADR-0124 状态 accepted 但用户只给过 P0 的口头放行；③四带重组的量化前提（DSH 侧栏 350pt/五者最宽）是截图文范围估算，像素实测 **279pt**、落在参照带 252–280pt 内（基座契约 `layoutInfo.sidebar` 初值 280，双来源互证；总账 P-51）。用户选择最严收口：**四条结构决策同批当场裁决并冻结**（决策票 S/T 十项，逐项点选）。决策：①四带冻结——新条目必须先声明归属带；②启动带沿用 ADR-0124，行高定案 **36px**，30pt 作废；③**split 恢复为 API 可选**（`position` 四值、默认 `stacked`；本条取代 ADR-0124 D5「split 列退役」的前提，split 形态的行轴判据在启用前必须补齐）；④能力带收进「工作台 ▸」折叠组（L1 入口 + L2 四项）；⑤内容带**置顶与搜索同进 P1**；⑥系统带冻结；⑦宽度 **264px**（`clampWidth` 区间 [264,420] 的下界；252 因越界被否——CSS 强改会让拖拽手柄与渲染宽度脱钩）；⑧入口纪律升门禁——官方 UI 锚禁止钉哈希 + 同列同行轴清单合写为入口契约，判据面新增**宽度断言**（此前缺口，P-51 登记）。验证：决策票十项逐项点选 + `pnpm run gate` 全绿；P1 实现未开始。 | accepted（2026-09-18） | [Note](../notes/implemented/surface/2026-09-18-sidebar-four-band-contract.md) |
| ADR-0126 | 更新安装闸按**行为结构**判，不按文案判（P0-1v2 从「补丁在不在」升级为「闸还成不成立」）：T-08a 在升级方案里标为「必做」却从未建立（全仓 grep `update-guard` 零命中）。装机真实字节上的两条突变暴露了「消息串仍在、闸已废」这一形态：闸整句挪到 `downloadDesktopUpdate(` **之后**、条件抽成 `if (false)`——而现有守卫 `ck "P0-1v2 更新守卫"` 判的是**消息串**，两处**都判绿**（M3 尤其隐蔽：紧跟的 platform 检查也带 `throw`，连「判后有 throw」都照样成立）。决策：新增门禁 `update-guard` + 反向自测 `update-guard-selftest`，判据为纯函数 `checkUpdateGuard(text)`；断言四条**结构关系**——闸用 `DSH_DISABLE_UPDATE_INSTALL` 且紧跟与 `0` 的比较 · 判完紧跟 `throw` · 闸排在 `downloadDesktopUpdate(`/`shell.openPath(` 之前 · `downloadAndOpenUpdate` 仍有调用点（否则闸是死代码）；射程复用 `patch-anchor-scope.mjs` 的 `selectAnchorTargets`（本机 `/Applications` ∪ 未打 tag 的 staging 树），不另立第二把尺；读不到或方法体不可解析报 `unverifiable` 并**计入失败**（含实测的 mode 000 → `EACCES`，属仪器问题而非补丁丢失）；与 `patch-anchors` 分工**不合并**——那条管补丁在不在（含消息串），这条只管它在行为上还成不成立。**备选方案丙被自己的突变推翻**：只判「串 + 顺序 + 有 throw」时 M3 判绿，补上「条件必须还在跟 `0` 比」才转红——判据的边界只能靠突变找出来，不能靠推想。验证：反向自测 12/12；装机真实字节绿；M2/M3/M4/M5 四条突变全红。诚实划界：静态文本断言**拦不住**「闸在原位、但上游在别处新开了一条未受保护的下载路径」 | accepted（2026-09-18） | [Note](../notes/implemented/security/2026-09-18-update-guard-behavioural-judgement.md) |
| ADR-0127 | 启动健康上报必须**确认送达**，不能赌「路由已就绪」：宿主放行界面要求 `phase==="monitoring" && nativeMounted && rendererHealthy` 同时成立，而 `rendererHealthy` 只能来自渲染器 POST `/_dsh/desktop/renderer-boot`——该路由由宿主插件在自己的 `ctx.effect` 里注册，**必然晚于** `renderer.boot.started` 就绪。实测同一端点 +6s→404、+10s→401，同一配置连续两次启动结果相反。渲染器原本只发一次 POST，落在路由就绪前即永久失败 → 宿主等满 30s 判 `renderer.boot.timeout` → 启动页一直转。修法：`postRendererBootReport` 改确认式重试（40 × 500ms），宿主判定一行不动 | accepted（2026-09-18） | [Note](../notes/implemented/packaging/2026-09-18-boot-health-report-retry.md) |
| ADR-0128 | 能力目录**只读聚合**，调度面挂 `shell.overlay`：五个能力切片（技能三线 223+138+16 / MCP 工具 101 / 岗位 50 / 产品 / 业务系统 31）此前各有各的搜索框、卡片语法与空态，「我想干 X 从哪进」在整个产品里无处可答。新增 `dsh-capability-hub-local`，五条决策：①聚合层永远只读——只 GET 既有 loopback 路由，不落第二份存储，404 记空切片（`missing`）而非错误，路由清单单一事实源 `CAPABILITY_ROUTES`，响应形状由 mapper 单测冻结（样本取自 handler 构造代码，上游改形状时测试先红）；②调度面挂 `shell.overlay`（帧级 additive，跨会话），**不挂** `conversation.input.overlay`（会话作用域的 composer 内浮层，是 `/` 命令源的家），模态一律 `<dialog showModal()>`、零 z-index；③action 是判别联合，执行只走既有通道（`dsh:skill-execute` / `setDraft` / `dsh:view-change` / open-system 只发 slug），预填不可用时降级剪贴板并上报原因；④间距与圆角走自建 `--lute-*` 命名空间（平台无 spacing/radius token 家族，`--dsw-alias-radius-*` 是门禁已登记的幻觉 token），`shared/client/lute-tokens.ts` 幂等注入且**消费方不得在 dispose 时移除该标签**（可能是兄弟插件注入的）；⑤`prefillDraft` 提为共享源，岗位矩阵改 re-export（第二个消费方出现即意味着两个家）。刻意不轮询（`/list` 已被胶囊组件每 2 秒轮询）。判据射程：不为「所有能力都该进命令面板」背书，低频配置类入口仍留设置页与系统带 | accepted（2026-09-19） | [Note](../notes/implemented/surface/2026-09-19-capability-hub-palette.md) |
| ADR-0129 | 管理层（MGT）Preset 以**评估载体**身份整合，不进出货面：材料仓（AI组织变革）2026-09-18 新增管理层 v2——3 个 CXO 岗位（MGT-001 持衡 CEO / MGT-002 驭浪 增长官·38 岗 / MGT-003 明界 治理官·12 岗），定性为**决策权平面**而非 Agent 层级（不与 50 岗会话、不进 Case、只读聚合、权力仅经 MDC/GOC 版本化契约生效）。三个硬约束塑造九条决策：DSH preset 是 Prompt 层表达而材料红线要求「只能 Prompt 层隔离时 MGT-003 不得进 Shadow」；MGT-EVAL-B 已执行**未通过**（阶梯归属一致率 66.7%）且 R0 对照臂未授权；存量 50 preset 的 shared_source_hashes 覆盖 8 个共享材料文件（动任一 = 全量重生成）。决策：①授权姿态 = 评估载体 + 本机装配，shipped exclude 档登记，出货前置 = EVAL-A/B + R0 + 另立 ADR；②`mgt-NNN` 独立命名空间（材料侧 MGT-Q1 关闭），PLN-EXC 为**投影层平面**（order 基座 0，家在 management-catalog `dsh_integration` 块），organization-graph 不得新增平面；③generate.mjs 双装载分支，eval 状态入 MGT 共享快照（材料侧验证状态更新自动传染披露块）；④MGT↔AGT 覆盖事实以 management-catalog 为家、不写共享图，存量 50 零扰动；⑤T0 定制子集（剔 meeting-minutes/xindaya-translator/kami）+ 16 管理技能名诚实映射（gap 为主照实披露）；⑥账本三处更新（exclude+3 / presetCount 56 / verify-lossless 双覆盖 + MGT 层），数量断言从材料 role_count 派生；⑦同轮 53 岗全量机械审计（research/16）处置：collaborates_with 33% 非对称判定为**有向边设计语义**（枢纽岗被指不回指，门禁只校验悬空）、T0 租户工具挂 50 岗登记独立债务、域/平面两轴正交互补文档说明；⑧材料侧 MGT-Q1/Q2/Q3 关闭登记 DECISIONS.md，G8/G9/Q5 保持待决。取证：审计发现 collaboration-graph 338 边中角色↔角色边 = 0（协作事实唯一住在 role-catalog）；skill-map 151 名仅 1 gap；产物层 order/icon/T0/披露段 50/50 零缺陷 | accepted（2026-09-19） | [Note](../notes/features/contract/2026-09-19-mgt-layer-preset-integration.md) |
| ADR-0130 | LUTE 面改乘基座**官方 slot**：中心列面板挂 `main` keyed slot、侧栏行挂 `sidebar.panellist`（id 逐字等于 key），DOM 注入行与自管浮层退役。自造承载的代价已全部落过账——P-07 行轴漂移、注入顺序由落位竞态决定（修法是让两份家族清单**有意不对称**，用清单措辞守本该由机制守的事实）、P-52 黑屏事故的根因就在注入/自愈这条路径上、层号魔数 2147482000；而基座本就提供官方座位（`renderSlot('main', {}, { entryKey })` + `entriesOfSlot('sidebar.panellist')` 自动派生行、`selectPanel` 对未注册 key 抛错=判据内建）。六条决策：①标准注册配方冻结（`id` 必须等于 `key`，label 写成函数由基座在 locale 切换时重算，图标组件只吃官方 owner props `size`/`active`）；②迁移后的面**不得自带层叠**（`z-index:` 声明 / `position: fixed` / `@keyframes` 均为 0，删自管层号变量），判据钉层号声明而非定位方式，`position: absolute` 仍可用于局部装饰；③「离开视图」只走官方 `layout.selectPanel(null)`，面板无关闭语义，**每一条**退出路径（返回/Escape/卡片执行后跳转）都必须走 onExit；④`sidebar-row-axis` 门禁只登记仍走注入的行，摘行必须同一次提交改它的反向自测 fixture（否则退役的行会被判成「未登记注入行」，红字指向不存在的东西），最后一个注入行退役时该门禁整体退役而非留成恒绿；⑤技能中心先走作为模式验证（三件全占：注入行+自管浮层+事件总线显隐），岗位矩阵与新应用重复同一配方。实测：注入行归 0、官方行 `aria-current="page"`、面板祖先链 `panelPage → centerCol → frame` 且零 fixed/高层号、退出后 composer 几何逐字复位；迁移当天撞到 spike 未预料的缺陷——卡片「执行」仍广播已无听者的 `dsh:view-change`(chat)，提示词交付了而面板还盖着中心列。⑥自造事件总线随迁移逐条退役——`dsh:skill-execute` 实测**全仓+基座零听者**却被技能卡与命令面板两处广播（P-04「写了但从没跑到」），两处一起删（只删一半等于让「这条通道存在」的说法在另一半里继续活着）；提示词交付只留 `shared/client/prefill-draft.ts` 的 `deliverPrompt` 一份实现（setDraft 优先→剪贴板兜底→两级降级都出声→成败如实返回），**交付前必须先取消 keyed 面板选中**（面板盖着 composer，否则草稿写进用户看不见的地方）；`open-panel` 的迁移判据与基座同源（`slots.entries('main')`，正是 `selectPanel` 该不该抛错的那条判据），不抄「哪些面已迁移」的清单，回退旧广播时必须出声点名目标 view。实测：技能卡「执行」后 composer 真的落进 `使用技能 /bestseller-pattern-decoder`；面板打开时从命令面板选技能，面板与 palette 双双退场且草稿可见；选岗位（目标未迁移）不误报成功、console 点名回退原因。判据射程：不为「所有面都该成为 main 面板」背书，也不改 ADR-0128 D2 对调度面（叠加层挂 `shell.overlay`）的判断 | accepted（2026-09-19） | [Note](../notes/implemented/surface/2026-09-19-skill-center-keyed-slot.md) |
| ADR-0131 | 品牌收口：出货产品改名 **WorldPilot**〔**名字部分已被 ADR-0136 取代：产品名实为 Sanbao / 三宝，WorldPilot 降为视觉语言供体；读本行前先读 ADR-0136**〕，LUTE 退为内部标识，换皮射程 **20 处含签名身份**。现状是三套半品牌并存（对外 `LUTE Agentic System`、侧栏 `路特创新 / AgenticOS`、boot 字标语义 `ROOT`）加**从未品牌化的基座残留**（`manifest.webmanifest` 仍写 `DeepSeek Harness`/`DSH`、`agent-team-gui` 用户可见文案仍要求「重启 DeepSeek Harness」——`brand-replay.sh` 只覆盖 app bundle 是既有射程缺口）。主色 `#58B848` 在盘上 4 个家 + 3 个校验器 + 1 个门禁锚文本，正是 P-07 的形状。素材源仓 `workforce.json` 的 AGT-001…050 与本仓 `role-catalog.json v2.0` 经逐条比对**同源**（职责 50/50 命中、花名 50/50 全等、四平面×八责任域一致），故本轮不是抄陌生品牌的皮，而是把同一产品的两套既有表达收口成一套。五条决策：①单品牌收口，不留底座名与皮名并存的中间态；②**改名只发生在显示面不发生在寻址面**（bundle 路径/可执行文件名/`appId`/包名/目录名/治理三元组不动——TCC 授权与数据目录挂在 `appId` 上）；③射程 16 处含签名身份，实测证书由 `ensure-signing-identity.sh` **本机自签**（无 Developer ID、无 notarization）故换身份成本是重跑脚本而非客户断链，射程表 `.scratch/sanbao-reskin/surface-map.md` 为唯一权威且每条须写现状值与目标值；④存量泄漏三处清零且**必须有一条常绿可红的判据扫新增**，扫描面为空判红；⑤**换皮只改表达不改承载**，ADR-0130 的 slot 挂载/事件通道/面板几何与 ADR-0128 D2 零 z-index 在换皮前后必须逐字节同形。被否方案：双品牌分层（契合 ADR-0061 但每条文案位都要回答「这句给谁看」，射程表从 16 涨到 32、P-07 从 4 个家涨到 8 个）、只借视觉不借名（W/P 字母标配 LUTE 名字互相否证）。需跟进：射程表本身是新事实也会腐烂，无判据则 D4 只做一半；`dmg-layout-doc` 入口表随 DMG 名/卷名同步否则 R3/R4 判红；旧签名身份证书是否保留决定老 bundle 能否复现构建 | accepted（2026-09-19） | — |
| ADR-0132 | 色彩与排印契约：**钛银单主色、1 套品牌 × 明暗双色道、材质解禁、`--sanbao-*` 为源并纳入门禁**。四条现状事实使换皮不能只做替换色值：①`#58B848` 有四个家（`brand.tsx:18`/`lute-tokens.ts:43`/`newapp.module.css` Brand block/官方 UI 补丁且后者是 `patch-anchors` 锚文本）+ 三个校验器复述；②`brand-css.test.ts:51` 断言前提是「遵循 **Codex** 视觉基线」——该人格正是本轮退役对象，而 WorldPilot 签名恰是材质（`--metal-fill: linear-gradient(145deg,…)`）；③`theme-tokens.mjs:57` 的 `NAMESPACE=--(?:dsw|ds|dsh)-` 使新命名空间对门禁**隐形**，其注释已预言后果「门禁看不见、页面照走字面兜底」；④S4 D3 的四套候选建立在「绿活着」的前提上，且 `midnight`/`graphite` 与钛银阶语义重叠。WCAG 实测：暗 accent 11.72:1（现状 6.99）、亮 7.62:1（现状 5.29），而硬编码回退字面量在亮色底仅 **2.33:1** 低于 UI 组件线——换皮是净改善；唯一代价 accent vs muted ΔE*ab 8.7。六条决策：①全量照抄契约（`#c4d0dc`/`#3d566e` + 中性阶 + `--good` + radius + 动效），`#58B848` 退役；②收敛为 1 套 × 明暗双色道，codex/graphite/midnight 降 legacy（不进墙、分享串仍可解析但提示迁移），**取代 S4 D3**；③拆混装断言——材质渐变合法化、带色相 `box-shadow` 辉光继续禁（现状绿光晕即反例）；④双轨 token：`--sanbao-*` 为源、官方别名为派生、`NAMESPACE` 扩含 `sanbao` 且**同一次提交**建定义之家，硬规则「同一条值不得两处都写字面值」；⑤默认 `uiFont` system→inter（基座已内置 6 个 woff2 子集，零新资产），`.tiny`/`.eyebrow` 提为共享排印原语，**不采纳官网 Arial 栈**（那是零依赖静态页的妥协，品牌字标面在 logo 包）；⑥**不纳入 `--kg-*` 七色板**——其键是 7 条 domains，与本仓渲染的 8 条 `FLOW-01…08`、材料仓 `mece_rule` 的 12 条三者互斥。已知风险：ΔE 8.7 须给真机可辨性读数，不达标走材质派生升级路径而非回退饱和色；改默认字体须实测复核 `sidebar-row-axis` 与 `TARGET_SIDEBAR_WIDTH=264`；`LEGACY_EDITORIAL_SIGNATURE` 按十六进制做解码期分支须验证不误判新默认。〔D2 双色道与 D5 标题紧缩签名的更新见 [ADR-0145](ADR-0145.md)；本行保留历史记录。〕 | accepted（2026-09-19） | — |
| ADR-0133 | 图形资产落位：**入仓 + pin 校验、logo 五形态映射、macOS squircle 重画、头像双轨承载、纯皮不动内容**。取证三条硬事实：①`build-app-icon.sh:22` 构建时读 `~/.dsh/skills/lute-brand-icons` 产出出货 `icon.icns`，而该目录实测 `fatal: not a git repository`、靠 `catalog.js.bak-pre-…-<时间戳>` 做版本控制——**ADR-0022 认证为「唯一事实之家」的东西不可复现**（P-01）；②素材源仓 `app-icons/*-1024.png` 是 **RGB / hasAlpha: no** 的硬边不透明方块，而 macOS 不做圆角遮罩，现状 `icon-1024.png` 是 RGBA 圆角——且 `brand-icons` 门禁只校 IHDR 尺寸不校形状，**错了照样绿**；③官方 loader 在 `index.js.patch:9` 白名单构造 record，`preset.yml` 多写字段被丢弃即 `icon` 是单值，而素材源仓是双色道×7 档且 README 自陈用错色道会显脏，`verify-lossless` L10 又要求 inline **SVG** data URI。另：头像全量 183M（原生 PNG 约 85M/色道、7 档 webp 仅 5.6M/色道）+ 源图 180M，而 ADR-0013 当年判定 32.9MB 单文件即应归档出树。六条决策：①最小集入仓（<3M）+ 新增 `vendor/worldpilot.pin` 哈希校验，不克隆只 pin；②五形态按上游使用规范逐位落位（启动屏 stacked、侧栏 mark 28×25 contain + 17px 字标、hero mark 34、tray 各档 + black/white 做 template、favicon 直落、PWA 接 app-icons）；③重画深色底 squircle，由可复现脚本产出并替掉废弃的 `LUTE_ICON_ENGINE` 分支；④头像双轨——官方卡放一份深色**兜底 data URI**（离线可渲染），自研面吃资产路径 + 主题/尺寸自适应解锁双色道，**不扩官方补丁白名单透传 iconLight**（本仓最脆面，ADR-0019 的存在理由），代价「官方卡浅色主题下仍是深色底头像」作为**已知残留**登记；⑤内容面冻结，只入 AGT-001…050（实测 102 条中 053…102 共 50 条是 `job: 待分配岗位`/`plane: 扩展成员` 纯占位，051 BoBo、052 筑程为已配置新增）；⑥不引入知识图谱 hero（7 节点即 7 域本体，与 mece_rule 12 条、产品 8 条 FLOW 互斥），只引入 `earth-texture` 纹理。明确否决：用 `<svg><image href="data:image/webp;base64,…"/></svg>` 包一层骗过 L10——P-02 仪器假绿 | accepted（2026-09-19） | — |
| ADR-0134 | 命名射程判据：**只改品牌位，功能面板名保留；品牌名不翻译**。判据逐字为「一个名字若同时承担『让用户找到功能』的职责，它是功能位，不在换皮射程；只有纯粹表明『这是谁的产品』的名字才是品牌位」。据此五个功能面板名（岗位矩阵/扩展中心/新应用/能力中枢/算法技能库）及副标题、空态、错误、操作提示**保留现状**。改名成本的两处误判先纠正：`live-presets` 的 `rowIdentitySha256` 哈希的是 `agent.cordis.yml` 每行 `stableId\0name\0kind`（56 preset/1788 行），而 `preset.yml` 显示名与 `locales.ts` 面板标签**都不在哈希里**——改哪一层决定要不要重采样，不能一概而论；本仓计划里另有铁律「反序会让用户在迁移期哪都找不到入口」。五条决策：①D1 品牌位/功能位判据；②D2 品牌名不翻译，全程只用拉丁字标 WorldPilot，「路特创新 + AgenticOS」两行结构退役；③D3 hero 用素材源仓中文叙述句「AI Native 经营决策与运营执行平台」、badge 用「面向跨境电商与出海品牌」——**照实登记该句是关于客户群的定位断言**，出货对象不符即为假，风险由射程表承担而非改文案规避；④D4 启动屏删除独立文本字标只留 stacked（其 SVG 已含路径字标，文本段成为重复事实），`brand-replay` 语义锚 `this.wordmark=…("ROOT")` 重切，代码内 `ROOT` 命名族属内部面不改；⑤D5 泄漏清零目标串取产品名本身（「请重启 DeepSeek Harness」→「请重启 WorldPilot」、PWA `name=WorldPilot`/`short_name=WP`）。附带修掉一个真缺陷：`Artificial Business Intelligence Agentic` 是三名词堆叠加悬空 `Agentic`、语法不成立，且在盘上有 4 个家。需跟进：功能面板名保留意味着产品内叙述（平面/责任域/岗位）与官网叙述（价值流/场景/经营链）存在语体落差，这是 ADR-0132 D6、ADR-0133 D6 的必然结果，须待内容轮统一本体后才能消掉 | accepted（2026-09-19） | — |
| ADR-0135 | 换皮的**门禁改造与执行契约**：单一品牌源 + 不可消除字面量纳入校验 + 六个垂直切片。三条硬约束使「全部归一」物理上不可能：补丁锚必须按精确文本匹配故只能是字面量；校验器的价值恰在独立复述期望值（`brand-css.test.ts` 注释「把版本钉复制进断言等于自证」）；`theme-tokens-baseline-frozen` 与 `exemptions.json`（当前 `[]`）都是只减不增，**没有任何豁免余量**给换皮。另两处结构性风险：三处品牌文案住在 gitignored 嵌套仓 `vendor/dsh-desktop/`（git review 不可见），而所谓「视觉基线」`.dsh-root-brand-preview/visual-baseline/` **完全未跟踪**故不可从仓库重建。八条决策：①建单一源 `--sanbao-*`，生成物走 shared-sync，测试与校验器改为 import 源做断言——只有源、生成物、补丁锚三类可以合法持有值；②**不可消除的字面量纳入校验**：补丁锚保留字面量但新增「锚值 == 源值」断言，做法与 `theme-tokens-baseline-frozen`/`exemptions-frozen` 一致，不为零字面量洁癖删掉交叉验证；③三处 vendor 文案一律走 bundle 重放声明（纳入 `brand-replay-selftest` R1–R6），不改只读 pin 源码；④新增 `brand-token-single-source` 与 `brand-name-leak` 两条判据，**扫描面为空一律判红**（P-15），且扫描范围必须覆盖插件包——那正是 S20 泄漏活了这么久的原因；⑤`brand-icons` 补 **alpha/形状判据**：现状只校 IHDR 尺寸，素材源仓 app icon 是 `RGB/hasAlpha:no` 的硬边方块，直接落 `.icns` 会**静默出货且门禁照样绿**；⑥六个垂直切片 S1 品牌源→S2 logo 资产→S3 头像→S4 命名→S5 打包链与身份→S6 收口，**S6 之前不得发版**；⑦不灰度、整体替换，分期本身即风险控制，回滚=重装上一版 DMG（依赖同号归档）；⑧验收三层（gate/gate:full + 真机三件套 + 视觉基线入仓可重建 + 20 位逐位对照表，行数少于 20 即未验收），**明确不做**「拿产品截图像素级 diff 素材源仓官网预览图」——语境不同只会造出恒红、最终被人手动放开的坏判据 | accepted（2026-09-19） | — |
| ADR-0136 | **名字与视觉语言解耦**：产品名定为 **Sanbao / 三宝**，WorldPilot 降为视觉语言供体；启动加载动画显式保留。取代 ADR-0131 D1/D2/D3 的名字部分与 ADR-0134 D2/D3。起因是 22 条决策收口后用户补充改名待办，暴露出一个未被检查的耦合——前面把「视觉语言」和「产品名」当成了同一件事。拆开算账：与名字无关、改名后仍有效的约占本轮 **60%**（色板、`--metal-*` 材质、`--ux-*` 动效、`.tiny`/`.eyebrow` 排印签名、**102 位头像**（AGT 编号同源与品牌名零关系）、双轨 token 机制、命名空间扩列、形状判据、泄漏判据、锚值==源值断言、33 处单一源收口）；绑死在名字上的约 **40%**（17 个 logo SVG 里 8 个把 `WorldPilot` 钉成矢量路径、`WP` 连笔标、squircle 生成器所画字母、favicon、`short_name: WP`、DMG 名、卷名、签名身份、品牌座两行、`vendor/worldpilot.pin`）。不处理即改名=重做两个切片的一半，且**恰恰在名字这条上失守刚建立的一处一家**。六条决策：①产品名 Sanbao/三宝，供体不得当产品名出现在任何出货面；②**建唯一品牌名源**（`nameLatin`/`nameZh`/`shortName`/`sloganZh`/`sloganEn`/`signingIdentity`/`bundleDisplayName`），字标 SVG、favicon、图标字母、`short_name`、DMG 名、卷名、签名身份、品牌座、`<title>`、`CFBundle*`、文档标题全部由它生成——改名=改一个源+重跑生成，字标转路径复用供体 `sources/trace.json` + `Inter-SemiBold.ttf` 配方；③**字顺分工**：拉丁管图形与文件名、中文管描述（汉字转路径需中文字体子集且字距体系不同，文件名含汉字有机编码与 `machine-path-baseline` 风险）；④本轮名源初值直接填 Sanbao、**不先发 WorldPilot 版**（既然不灰度且中间不发版，生成一套只活几天的字标是纯浪费），slogan 取代供体叙述句并**消掉 ADR-0134 的 R3 定位断言风险**（愿景式口号非可证伪断言）；⑤图形字母标另立一轮，本轮用文字版 + 中性几何占位，`WP` 标不沿用；⑥**启动加载动画列为必须保留项**——实测其弧色取自 `--dsw-alias-brand-primary`（ADR-0132 D4 覆写的正是它）故自动跟皮、零改动，且 `brand.tsx:180-186` 的运动抑制作用域限定在 `[data-plugin="dsh-root-brand"] *`、只压 `transition` 不压 `animation`、碰不到 spinner；新增三条断言校验 `animation-name=spin`/`0.8s`/弧色解析值。主要欠账：本轮字标是文字版非设计过的字母标（待办 T1） | accepted（2026-09-19） | — |
| ADR-0137 | 品牌 token 命名空间改判为 **`--sanbao-*` 全拼**，并记录一次「pivot 后未审计已落盘文档」的自伤。ADR-0136 把产品名改成 Sanbao 后回扫六篇 ADR，查出六处 pivot 前残留，其中两条**实质自相矛盾**：①`--wp-*`（`wp` 即 WorldPilot）被 ADR-0132 D4 定为品牌色唯一源且会编译进出货 CSS——而我在 ADR-0132 备选方案表里用**逐字相同的论证**否掉了 `--lute-*`（「`--lute-brand` 里装着钛银，与 ADR-0131 D1 在代码层自相矛盾」），同一论证现在判我自己出局；②ADR-0132 D3 把测试新名写成「遵循 **WorldPilot** 材质契约」，而我改这条测试的原始理由正是它旧名「遵循 Codex 视觉基线」的前提是已退役品牌——只是把一个过期品牌名换成另一个。另四条是传染面没跟上：ADR-0131 机读块仍写「射程 16 处」（正文已改 20）、仍写「出货产品即 WorldPilot」，索引行同错，`.scratch/worldpilot-reskin/` 目录名被六处当作权威路径。**最难看的是 16/20 分叉**：上一轮改判只改了正文没改 `## 机器可读决策`，于是 `decisions.json` 同错，而 `adr-agent-records` 门禁**照样绿**——它比对「账本 vs 源」，无力发现「源自己内部不一致」，正是总账 P-02 仪器假绿，且由我本轮亲手复现。四条决策：①命名空间定 `--sanbao-*`（否 `--sb-*`：sb 是常见缩写可读性弱；否 `--brand-*`：与官方 `--dsw-alias-brand-primary` 的 brand 字面重叠，grep 易混）；②测试新名改「遵循 Sanbao 材质契约」；③**被取代的 ADR 不静默改写**——机读块保留历史原文并追加〔已被 ADR-0136 取代：…〕，因为只改正文头部一行指针不足以让读账本的 agent 知道真相（ADR-0122 立账初衷就是开工前必读），但直接改写历史决定会让「当时怎么定的」永久失传；④改一条品牌事实必须同一次提交扫四个传染面（正文/机读块/索引行/派生账本）+ 路径引用，且**不得以门禁绿作为一致性证据**。刻意保留：`vendor/worldpilot.pin` 不改名（它 pin 的确实是 WorldPilot 仓，名字准确）；ADR-0131 标题不改（不可变记录，靠状态行+机读块+索引三层标注）。未闭口：D4 目前仍是纪律不是门禁，「正文与机读块关键值不得互矛」这条判据难写对（「关键值」无通用判据），留作待评估不承诺 | accepted（2026-09-19） | — |
| ADR-0138 | 引入 **Jev**（TypeSafe System One 判断模型）作 SkillOpt 的 **Tier 1.5 语义判读层**。三条不可绕过的实测事实决定了形态：①三宝是 Electron 本地优先**无服务端**，出货面直调付费第三方即必然把 key 打进 app bundle 且数据出境；②`~/.dsh/.credentials.yaml` 官方 README 自陈「**该存储无法向 agent 隔离机密**」，而实测 64% 的 agent 步在跑 bash；③`~/.dsh/scratch/attrib/` 是 43.4MB+18.8MB 完整会话转录、含 `danger-full-access`、前 300KB 命中密钥形态。另有一条决定性的：**这件事已经有家**——`skillopt-optimization-report.md`（246 个全量静态评估 + `triggers` 建议）、`skillopt-tier1-apply.py`（建议→机械校验→落盘→双快照）、`skill-contract-plan.md`（frontmatter `example`=真实业务场景示例）在做同一件事，而该报告 §三 结论「静态评估永远发现不了文档-实现脱节」是本项目自写，故 Tier 1.5 明确**不接管轨迹层**。八条决策：D1 落点限内部研发回路；D2 出网上限=仓库内 git 跟踪文本、attrib 永久禁止；D3 并入 SkillOpt 复用 `sug-*.json` 与 apply 链、不新建第二个家（P-07）；D4 判据主集三条（逐任务 Noul 由代码算**越界率**、无例外停止、**收窄后的**环境机制依赖），④高影响未确认因实测无方差被逐出主集（留著即恒绿，P-25）；D5 门禁判**内容寻址输入指纹**不用时钟，使第三方不进 gate 关键路径；D6 钉 `jev-1.13.0`+阈值与判据住单文件；D7 凭证 `LUTE_JEV_API_KEY` 进既有 `refs`、只读、风险显式登记；D8 双轨验收（离线工程轨进 gate、语义记分卡只进报告）。两轮真实实验：①从「正例 0.44≈真实 0.45 无区分度」经换用仓库内真实电商场景句拉开到 0.34/0.39 vs 1.74/1.14，同时暴露它测的是**相关度不是宽度**；③未收窄时把两条**已修好**的语料判成 0.90/0.81，收窄后 0.73→**0.06** 而缺陷样本 0.96→**0.97**。成本 1265tok/条、292 条≈$0.0155——钱不是约束，判据质量才是。未闭口：D2 边界仍是纪律无门禁；key 对所有会话可读只登记未消除；项目级 skill 被 `.gitignore` 通配 `*` 排除故不随克隆走且与全局副本重复（官方建议二选一）；阈值全为待校准初值（②边距仅 0.33）；SkillOpt 的 `ROOT=~/.dsh/skills` 与仓内 292 条 corpus 不是同一份，Tier 1.5 跑哪份未决 | accepted（2026-09-19） | — |
| ADR-0139 | **LUTE 自有 Electron 薄壳（P1 骨架）的四条落点决策**：宿主走子进程 + FD3/FD4 的 DSH3 v3 帧协议、profile 用「仓库 seed + 运行时物化」、宿主运行时就位在 `<profile>/lute-host/`、门禁只加静态 `lute-shell-pin` 不扩 package collector。两条现实约束决定了形态：①**消费 npm 上的宿主这条路不存在**——`@deepseek-ai/dsh-desktop-host` 实测 `ERR_PNPM_FETCH_404`，宿主入口只能自己持有；②npm 的 `latest` tag 指向 `0.0.1-rc.*` 旧线（`pnpm view @deepseek-ai/dsh-cmdline version` → `0.0.1-rc.1`，而 `dsh-web-app@0.1.5-rc.2` 依赖 `^0.1.5-rc.2`），故 seed 的 13 个 `@deepseek-ai/*` 一律**精确 pin**、出现 range 即判红。拒绝「主进程内 boot」的理由是**观测旁路**而非性能：AI 运行时与 UI 同生共死时「主线程挂了」与「宿主挂了」在唯一仪表上同形，正是总账 P-52 的形状；拒绝「harness 自带 HTTP」是因为它走浏览器客户端路径，而诊断手册登记的两个已知客户端故障都在这条路上。D3 的单一 cordis 实例是**服务身份**问题：宿主若从仓库侧解析出第二份 cordis，注册与查找跨两个实例，症状是「明明 provide 了却 get 不到」，故 `nodeLinker: hoisted` + `lute-host/` 内不放 `package.json`（ESM 身份由 profile 根的 `"type": "module"` 决定）两条一起保。D4 八条守卫里两条是踩坑后加的：**空依赖集判违规**（空分母让两道版本守卫同时静默却仍报 pass，对齐 `gate-result.mjs:116` 的 `checked > 0` 教义，P-15）与**参照缺失跳过必须进 note**（不得静默判绿）。附带更正 [17 号报告](../research/17-thin-shell-spike.md) §3 的根因猜测：`layers: 0` 的真因是 profile manifest 缺 `dsh.profile.bundles`（`packages/boot/app-boot/src/profile.ts:781`），与 pnpm workspace 结构无关——实测 `layers: 2`。已兑现：新壳壳层补丁 0（旧壳 12）、自有源码 1680 行、无头 smoke 10/10 PASS。待跟进：`apps/` 不受 collector 治理（治理三字段是第二份实现，P2 重估）、`agent-presets` 系统根注入被跳过（P2 用真 preset 复验）、两个 native directory-picker 未插入、smoke 不进 CI 故「薄壳还能 boot」无常驻读者、`DSH_HOME` 未隔离（首屏显示本机真实会话，隔离属 P4）、spec §6 三条新门禁本期有意未上（终审修复波另收口了 `lute-shell-pin` remediation 的过时 ADR 指针，事实家见 18 号报告 §6.9） | accepted（2026-09-19） | [Note](../notes/implemented/architecture/2026-09-19-lute-shell-skeleton.md) |
| ADR-0140 | 右边侧边栏重构：统一扁平布局与 7 个能力模块 | proposed（2026-09-20） | — |
| ADR-0141 | **右边侧边栏 UI 优化：Qoder CN 风格**。四条决策：**D1** 折叠分组（工作区/工具/信息三组）；**D2** 尺寸规范（图标 24×24、组内 8px、组间 16px）；**D3** 质感（双层边框 + 微渐变 + 柔和阴影 + 玻璃态）；**D4** 激活态。2026-09-20 补记两处与已接受契约的冲突，均在实现中改判：①**D4 的「发光边框」与 ADR-0132 D3 冲突**——那条决议把装饰性彩色辉光判为**禁止**（当时给的正例是 `client.js.patch:7` 的绿光晕），故激活态改为「主题 accent tint 填充 + accent 描边 + accent 图标色」，无任何带色相 `box-shadow`；②**D1 的折叠分组未实现**——出货的是 7 行扁平注入（`MODULES` 平坦列表、`position: 'after'`），折叠分组留在备选里未做，索引行按「已实现的形态」登记而非按提案登记。附带一条同类收敛：`--lute-brand` 绿字面量从本包样式中全部撤出（激活/悬停/徽标/面板标题图标），一律改指 `--dsw-alias-state-business-primary` 与 `--dsw-specific-sidebar-nav-*`，使注入行与官方行同源跟皮 | superseded（2026-09-20，见 ADR-0143：折叠落在右侧面板，左栏保持扁平） | — |
| ADR-0142 | **岗位名片版式与官方预览角标移除**：三宝换皮做完资产层（50 张双色道头像）与渲染面（`object-fit: cover`）之后，真机反馈暴露了一个排期上的合并错误——换皮计划把「资产 / 渲染面 / 版面」当成一层，实际是三层；头像铺上了，**版面还是换皮之前那张卡**（34px 圆头像 + 姓名/岗位 + 一排徽标 + 标准产物行，读不出「一个人」）。三条用户原话各对应一处版面事实：①「把 `Preview` 字样去掉」——官方 `hero.preview` 角标，ADR-0019 D2 当年「保持节点、文本改成 `Preview`」是为「一个叫 Preview 的产品」服务的，三宝不处在该状态；②「字号跟实际页面完全不搭、层级要深度调整」——11/12/13/14px 四档摊在五类信息上，实际页面正文是 13–14px；③「头像太小、要像名片：左上角头像、下方工号、右边职责简介」「卡片框调大、一行 3 个」。五条决策：**D1** 角标整节点隐藏（`display:none` + `dsh-rb-hidden`，文本原样不动、卸载还原），`official-text.ts` 删除、文案常量迁入 `hero-title.ts` 作排除表；角标仍是定位官方标题的锚，且**标题候选歧义时角标照旧隐藏**（歧义针对「谁是标题」，与被正向识别的角标无关）——取代 ADR-0019 D2。**D2** 名片版式：左栏 96px 圆角方形（14px）头像槽 + 居中工号（mono），右栏姓名 16/500 · 岗位 13 · 两行职责 12 · 徽标；`标准产物` 移入展开详情（信息不丢、不挤名片）。**D3** 栅格 `repeat(auto-fill, minmax(340px,1fr))`、面板 `min(1200px,94vw)`：1440px 视口实测 3 列 × 376px，更窄降 2/1 列，**降列不降字号**，不写 `@media` 死列数断点。**D4** 五级字号阶梯（20/16/13/13/12/11）全走 `--dsw-font-*`，写在样式文件头并有用例钉住。**D5** 一句话职责由纯函数 `card-brief.ts` 从官方 `description` **只做删除**（前导【…】/结尾〔…〕/第一个圆括号之后的补充说明与残留标点），删空则原样返回——按字数硬截断得到的是「另一句话」，读者分不清是截断还是原文。附一条测试仪器的沉睡缺陷：`live-anchors.spec.ts` 的 `installPlugin()` 从未被 dispose，而观察器挂在 `documentElement` 上、`afterEach` 清 `body` 摘不掉它，于是上一个用例的插件继续对下一个用例的 DOM 动手且记下别人的「隐藏前状态」——修法是把装载过的插件登记进实例表由 `afterEach` 统一收口。验证（重启后 runId `5cc8220c-…`、`rendererStatus=healthy`）：hero 可见文本里 `预览版`/`Preview` = 0、角标 `display:none`、`anchors=resolved`；面板宽 1201px、首行 3 张、卡宽 376px、头像槽 96×96/radius 14px/cover、工号居中且在头像下方；字号真实解析 20/16/16/13/13/12/11；明暗两主题各 100 张 webp / 50 可见 / 50 已解码；搜索 `agt-050` → 1 张、空结果 0 张；页面错误 0。 | accepted（2026-09-20） | [Note](../notes/implemented/surface/2026-09-20-role-card-business-card-and-preview-badge.md) |
| ADR-0143 | **右侧工作台：一个面板装 7 个可折叠分组，折叠机制照 Qoder CN**。背景是用户给出新参照（Qoder CN 右侧栏是一列可折叠分组）并要求「折叠功能照它做」，同时排查此前质感改版「没有任何效果」——真机读数 `position: static / radius 0px / border 0px / y=923`。根因两处同源：①包里存在**两份** `right-sidebar.module.css`（`styles/` 质感版与 `src/styles/` 旧版）②`panel-shell.tsx` 多一层目录，`'../../../styles/…'` 解析到旧版，于是**面板壳用旧样式、模块内容用新样式**，类名映射（`GqBeGq_`）与 CSS 文本（`_7kP-uW_`）来自两个家（总账 P-07）。五条决策：**D1** 右侧栏 = 一个「工作台」面板 + 7 个可折叠分组（顺序沿用 ADR-0140 D2），左栏 7 行**仍扁平**（D1 有效），点行 = 打开 + 展开 + 滚到该分组，分组内容首次展开才挂载、折叠后保留在 DOM；**D2** 折叠照 Qoder CN——整条表头是 `aria-expanded/aria-controls` 按钮，内容用 `grid-template-rows 0fr↔1fr` 过渡（**不写死 `max-height`**，旧实现会在 1200px 处静默截断），chevron 展开朝下/折叠朝右，状态记 `localStorage['dsh-right-sidebar:collapsed-groups']`，`prefers-reduced-motion` 下过渡归零；**D3** 全包只留一份样式表与一份模块目录（`sidebar-entry.ts` 的 `MODULES`；`rowAttribute`/`position` 字面留在该文件供门禁发现），导入路径深浅不得改变用哪份样式；**D4** 外框按**渲染值** ≥1.2px 写——面板与分组卡声明 1.5px（2x 屏上声明 1.2px 被吸附成 1px，比要求更细）；**D5** 验收只认真渲染进程读数（`accept:right-sidebar`，CDP 打开面板读几何/结构/折叠行为/记忆键；前置缺失 exit 2 且打印候选，判据不过 exit 1）。本轮 22 条判据全过：面板 fixed / 30px / 1.5px / 视口内（y=14 h=895），7 分组 `aria-controls` 全可解析，折叠 119px→0→774px，分组卡 20px / 1.5px。取代 ADR-0141（其左栏分三组折叠未采纳）。 | accepted（2026-09-20） | [Note](../notes/implemented/surface/2026-09-20-right-sidebar-workbench-folds.md) |
| ADR-0144 | **配色改判为 Qoder CN 中性灰 + 灰绿（取代 ADR-0132 的配色部分）**。现状是两套色系并存——主区/左栏走 Sanbao 契约（深海军蓝 `#0b1521` 底、`#142332` 面板、钛银 `#C4D0DC` 强调），右栏工作台是照 Qoder 抄来的中性灰（`#202228 → #16181c`，CSS 硬编码、不跟主题走），且两套立面模型彼此相反（我们越抬越亮、Qoder 越抬越暗），并置即脏；用户对蓝的评价是「很讨厌」并要求 Qoder CN 的配色。三条决策：**D1** 配色契约改判——暗色道取 Qoder CN 实机实测值（rail `#242624` / 画布 `#232523` / 面板·顶栏 `#191B1A` / 卡片·浮层 `#131413` / 列边界 `#171918`），**钛银与深海军蓝退役**，灰绿 `#5C9363`（选中 chip `#609966`）重新登记为品牌强调色；取代 ADR-0132 **D1 的配色部分**，其余（1 套品牌 × 明暗双色道、材质解禁、`--sanbao-*` 命名空间与双轨 token、Inter 默认字体、不纳入七色板）继续有效；明色道照抄 Qoder 真值不推导，`#5C9363` 落地前须用第二个来源交叉核对（截屏采样，偏差 >2 阶取更可靠者）。**D2** 立面模型反转为 Qoder 式：画布最亮、面板/卡片/浮层越抬越暗，反转落在 `theme-tokens.ts` 的派生式（`bg-layer-2/3`、`bg-module-platform`、`bg-overlay` 由提亮改压暗），token 名与层级数不变、不在消费面写死颜色。**D3** 本轮只动主题包 `dsh-theme-local`（默认色值 + 派生式 + 单测），官方壳只换 token 不改结构，vendor 只读（ADR-0008）、官方 UI 改写锚禁钉哈希（ADR-0019）；不改 `TARGET_SIDEBAR_WIDTH=264`、不并行动右栏包（spec D7）。被否的路：保留钛银只换灰阶（蓝相冷银留在暖灰画布上，用户点名讨厌的正是这个蓝）、从暗色道推导明色道（把未知当已知，真值一到重做一遍，P-07）、只换色保留旧立面（立面方向与配色是同一条契约的两半）、同片收密度（换色与改契约必须分开）。验收：实机采样四值与 spec §2.1 逐值一致 + 对比度读数（强调色 on 画布 ≥ 4.5:1、正文 ≥ 7:1），不是「看起来像 Qoder」。 | accepted（2026-09-20） | — |
| ADR-0145 | 全站三主题身份与语义颜色单一源；旧双色道、仅主题包与分批交付限制被取代，保留字体偏好并逐叶验收 | accepted（2026-09-20） | [Note](../notes/features/contract/2026-09-20-global-theme-triad.md) |
| ADR-0146 | [右栏宽度设置口：官方 setRightbar 能力探测 + 区间镜像外壳 + 采纳只认拖拽](ADR-0146.md)：偏好唯一家在 localStorage，未设不写；两端对钉拦镜像漂移。 | accepted（2026-09-20） | [Note](../notes/implemented/surface/2026-09-20-rightbar-width-setting.md) |
| ADR-0147 | **Sanbao 改名落地四步成套；品牌重放改扫描式 + 曾用名**。装机 app 从 LUTE Agentic System 改名为 Sanbao，四条既有工具首次成套执行即连撞三处缺口：①「9 文件清单」漏了 8 个带品牌的文件（真机 93 个文本文件里 21 个带名）；②上一位品牌名只报不改（显示名块 PREV 分支在 `--apply` 下也报 DRIFT 不动手；`index.html` 替换锚写死上游名，PREV 态 `replace` 不命中**却打印 `patched`**）；③helper 与 CFBundleName 的匹配从未被核对（旧目录名写死首代名）——外层已是 Sanbao、helper 还叫上一位的名字，启动 17ms 崩 `FATAL: Unable to find helper app`（Electron 按 `Frameworks/<CFBundleName> Helper.app` 查找）。五条决策：**D1** 落地 = 四步成套、缺一不可（身份表 `productName` 两个家 → 数据目录迁移**只复制不删除**（rsync 排除 `Singleton*`/`DevToolsActivePort`，旧目录留作回滚后路）→ 显示名/Helper/Info.plist 重放 → 同身份重签（`LUTE Code Signing`；TCC 按 bundle id + 证书 leaf 记账，同身份重签后授权延续））；**D2** 品牌重放退役文件清单、改**扫描式**（app 本体文本文件、排除 node_modules 与 *.orig*；「曾用名」清单 `PREV_NAMES` 是唯一家；豁免按行判定显式报 SKIP——`productName:` 行归身份补丁、`app.setPath("userData")` 行是路径；基座平台名 `DeepSeek Harness` 不替换，仅窗口标题位由 2b 块认任意 `<title>…</title>`）；**D3** helper 目录与 CFBundleName 同批换、认曾用名、四处一起换（目录 / `CFBundleName` / `CFBundleExecutable` / `Contents/MacOS` 可执行文件名），「都没找到」静默跳过、「找到却换半」判红；**D4** 写路径默认带落笔后校验（标题 grep 复核、helper 回读 + `-x`、扫描器写后残留统计）——「没命中却报成功」与 PlistBuddy 静默失败是本轮两次假绿的形态；**D5** bundle 目录名 / `appId` / 签名身份不动；app 内 node_modules 的三方包品牌串为已知边界（需 vendor 化）。落地读数：`verify-patches-v2` 翻 `ALL VERIFIED`、lifecycle 写入 Sanbao 目录且 `rendererStatus=healthy`、日志尾行 `dsh-plugin-desktop Sanbao 2.0.10`、TCC 2 项身份型仍匹配、`brand-replay-test` 18/18（含新增 T/U 系列）与 `dsh-running-test` 11/11（含 R6d——修掉 `--any` 的 `index()` 撞号假阳性）。 | accepted（2026-09-20） | [Note](../notes/implemented/surface/2026-09-20-sanbao-identity-rename-landing.md) |
| ADR-0148 | **深度分析 TOP20 批次 A：五条机制债收口**。P0 五项全是 pitfalls-playbook 自认「尚无 / 只有纪律 / 缺口不是成绩」的债，整批关闭：**D1** 聚合层三态（P-17）——汇总行渲染迁进 `gate-result.mjs` 的 `renderGateSummary()`（住 `gate.mjs` 的逻辑没有注册项测得到），「未核对 N 项（不是通过）」独立成句并点名，`--require-no-skip` 与日常宽容语义分开写清；**D2** 权限位判据（P-45）——`gate:permission-bits` 射程 = 改动面四来源并集（刻意不全仓），判「读位全空（000/020/040/060）」与 `R_OK` 被拒，点名「文件 + 权限位」，记账单位 = 可判文件；**D3** 入库面占位化（P-48）——写入者直写占位符（`__DSH_HOME__` 语义 = `~/.dsh`）+ `gate:intake-placeholders` 逐行判 `/Users/` 与 `$HOME`，与出货链 `rewrite-build-paths` 划清；**D4** 清场三步序（P-49）——`cleanup-inventory.mjs` ①承诺（发布清单/`uchg`/门禁射程/ADR）→②引用→③体积，体积只在①②都为否时出场；**D5** 单例计数探针（P-52）——`singleton-count-live.mjs` 三类单例数到 2 点名「重复挂载」、读不到 exit 2、`--self-test` 9 状态 18 断言，共享层注释加读数指引，被证伪仪器登记进 `dead-instruments.json`。五条均配恒真桩突变验证。 | accepted（2026-09-21） | [Note](../notes/implemented/contract/2026-09-21-p0-mechanism-debt-batch-a.md) |
| ADR-0149 | **出货架构正式定为 arm64-only**：LUTE 2.5.0 单列的 x64 缺口转为决策——单一产物 mac-arm64.dmg、Intel Mac 不在支持范围（不经 Rosetta）；**逃生路径预置**（出现 Intel 消费方时：`MACOS_UNIVERSAL_NATIVE_ENTRIES` 登记 fs-ext darwin-x64 prebuild + `--dir --universal` 适配，验证面含 Intel 启动、fs-ext 加载与运行时物化+接入 SDK 的 intel 兼容性）；不触碰签名与发布口径。 | accepted（2026-09-21） | [Note](../notes/implemented/contract/2026-09-21-single-arch-arm64-decision.md) |
| ADR-0150 | **上游平台名 `DeepSeek Harness` 从可见 UI 面清出（brand-surface 补丁）**：三处（页眉 `productTitle` / 模型页内测声明 / 主进程远程控制对话框）逐处点名替换；补丁锚点唯一性判据（期望命中数 + 写后残留为 0）+ 6 条验收（3 正 + 3 否）；**生效判据 = 重启后服务端实际发出的字节**——运行中改盘不算（宿主按引导期快照发 app 内客户端 bundle）；身份/路径/签名与本补丁无关，CLI 描述与三方包描述等「正确指代上游事实」的位置不动。 | accepted（2026-09-21） | [Note](../notes/implemented/surface/2026-09-21-upstream-brand-surface.md) |
| ADR-0151 | **更新 feed `latest.json` 定为派生契约**（DA-10①）：`schema_version` + 10 个固定字段、固定顺序、snake_case、未知字段判违规——六个字段逐字段派生自 `release/<版本>.sha256` 入库清单（ADR-0058），`min_os` 读产物 `Info.plist`（缺键则打包中止），`channel ∈ {stable,canary}`；**禁止手写**（发布脚本 §7.5 生成 → 归档 `uchg` 锁 → 回读）；判据 `update-feed` + `update-feed-selftest` 进 quick 门禁，射程 = `release/` 全版本、**最新版本必须已有 feed**（更老缺失豁免、射程为空判红），六字段「分家」必须两个读数都点名；档案侧 `release-verify.sh` 核对 sha256/dmg/version（无 feed 记「机制引入前」）、F1-F3 三例、恢复链纳入 `latest.json`；**边界：不含 Ed25519 签名、不含自动下载/安装**（空壳字段会被读成「已签名」）。 | accepted（2026-09-21） | [Note](../notes/implemented/contract/2026-09-21-update-feed-contract.md) |

| ADR-0152 | **更新器骨架只做「检查 + 提示」**（DA-10②，包 `packages/platform/dsh-update-local`）：本机版本口径 = app `Info.plist` 的 `CFBundleVersion` 后缀（`2.0.10-lute.2.5.0`→`2.5.0`，无后缀是具名状态 `current-not-lute`；XML 文本解析不用 plutil 子进程）；**八态判定**（update-available / up-to-date / feed-behind / channel-mismatch / feed-unreadable / feed-invalid / current-unreadable / current-not-lute）——**读不到绝不折叠成「已是最新」**；工具 `upd_check` 只读、启动检查延后 20s 且离线只记日志；消费侧 `lib/feed.js` 与生产侧 `scripts/lib/update-feed.mjs` 靠用例**交叉钉**（对钉生产侧产物的键序 + 同一张突变表）；**边界：无下载、无写盘、无安装路径**（自动安装等 Developer ID）；装配走 profile 本地（ADR-0061）。 | accepted（2026-09-21） | [Note](../notes/implemented/contract/2026-09-21-updater-skeleton.md) |

| ADR-0153 | **降级自报标志定为「挂起 ≠ 失败」**（DA-21，包 `dsh-newapp-local`）：放置走 rAF 而窗口不可见时 Chromium 冻结 rAF——宽限到点若 `document.hidden` 即**暂停**（不报、不对休眠显示器轮询），`visibilitychange`（监听在 `document`，该事件不冒泡到 window）恢复后给 1200ms 短宽限再判；**标志不得比它的条件活得久**——迟到归位时清除 `entry-unavailable` 并拆除定时器/观察者/监听；「可见 + 宽限过后仍无入口行」照旧响亮降级（**不许拉长宽限期掩盖**）。 | accepted（2026-09-21） | [Note](../notes/implemented/surface/2026-09-21-newapp-degrade-window-hidden.md) |

> ADR-0144 的配色源、亮色立面与仅主题包范围已由 [ADR-0145](ADR-0145.md) 更新；其原记录保留为历史。

> ADR-0007 ~ ADR-0018 是「LUTE 二开平台架构重构」的十二项决策，共享同一篇决策记录 Note。
> ADR-0019 独立成篇（品牌皮肤锚点治理），决策记录见其 Note。
> ADR-0020 独立成篇（岗位小队编队契约），决策记录见其 Note。
> ADR-0021 独立成篇（preset 供给披露规则），决策记录见其 Note。
> ADR-0022 独立成篇（岗位头像契约），决策记录见其 Note。
> ADR-0023 独立成篇（preset 清理与默认值迁移），决策记录见其 Note。
> ADR-0024 独立成篇（上下文压缩加固），决策记录见其 Note。
> ADR-0025 独立成篇（宿主路由注册契约 + 替身即契约纪律），决策记录见其 Note。
> ADR-0026 独立成篇（官方 UI 覆盖规则的顺序无关性），决策记录见其 Note。
> ADR-0027 独立成篇（模态浮层的 top layer 纪律），决策记录见其 Note。
> ADR-0028 独立成篇（启动器的所有权边界与半宽并排几何），决策记录见其 Note。
> ADR-0029 独立成篇（主题 Token 的证伪必须落在真实引擎与真实供给两侧），决策记录见其 Note。
> ADR-0030 独立成篇（第三方插件在 `/api` 下注册 exact 路由会压过平台的 `/api` 前缀栅栏，来源栅栏须逐插件补），决策记录见其 Note。
> ADR-0031 独立成篇（技能分类与岗位矩阵同构 + 缺口认领自洽），决策记录见其 Note。
> ADR-0032 独立成篇（跨框架注入共享核心的「未挂载」表示唯一化 + 注入契约须在真框架环境证伪），决策记录见其 Note。
> ADR-0033 独立成篇（Native Agent 产品的声明之家与启动器的发现/开入口边界），决策记录见其 Note。
> ADR-0034 独立成篇（产品的功能骨架分层与步骤级分工，M1 试点压出的修订），决策记录见其 Note。
> ADR-0035 独立成篇（效果断言的实测背书要求 + held-out 门与回滚纪律，由 L6/L7 实测压出），决策记录见其 Note。
> ADR-0036 独立成篇（产品包的挂载层契约），决策记录见其 Note；其决策 3 已由 ADR-0037 取代。
> ADR-0037 独立成篇（产品包两层挂载与 `role: entry` 关断机关），决策记录见其 Note。
> ADR-0038 独立成篇（共享栅栏「拒绝而非抛异常」），决策记录见其 Note。
> ADR-0039 独立成篇（语义 Token 三条法则与门禁引用口径），决策记录见其 Note。
> ADR-0040 独立成篇（开发脚本子进程的解释器必须是真 node，判据须先剥注释与字符串），决策记录见其 Note。
> ADR-0041 独立成篇（分类的运行时家是已安装的技能本体；归位与接线分列），决策记录见其 Note。
> ADR-0043 独立成篇（门禁失败证据按流分流、退出码不得折算），决策记录见其 Note。
> ADR-0045 独立成篇（产品矩阵剪枝 + 卸载 dsh-worktable），决策记录见其 Note；它部分取代 ADR-0028 与 ADR-0033 第 6 条。
> ADR-0046 独立成篇（可选服务读取纪律 + Remote 三元组的传参与返回值校验），决策记录见其 Note。
> ADR-0062 **部分修订 ADR-0045**：产品分区的目标函数一字未改，抽屉只是多了一个「业务系统」分区；
> 被改掉的是「这个抽屉只放产品」的收口，以及 M1/M2 的适用范围。理由与实测读数见其 Note。
> ADR-0047 独立成篇（p2s 语料三行级缺陷改在流水线里修 + 出处按档位渲染），决策记录见其 Note。
> ADR-0048 独立成篇（p2s 卡 ⑦ 段是节选不是模板 + 卡页路径只作转述），**部分取代 ADR-0047
> 「影响 · 代价与边界」里的两条**：那句「逐字节相同 1,283/1,283」应改按三类计（1,082 相同 /
> 188 仅末尾纯空白差 / 3 处刻意脱敏）；「787 条悬空路径」量错了基准（换全量树后 838/838 全解析），
> 且路径指向的代码树与本卡节选同名不同物。
> ADR-0049 独立成篇（完整实现从语料 vault 的 git 明文恢复 + 落 references/implementation.py），
> **订正 ADR-0048 的两条全称断言**：那句「卡的 code_path 838/838 全指向无编号树」有 **18 条反例**
> （18 张卡指向带编号的代码代）；那句「预览是这份代码唯一存世的地方」也不成立 ——
> iCloud 工作区的 1,353 个 vault `.md` 已是被改写的二进制容器，但**同一仓库的 git 历史里 1,338/1,338
> 张卡都是明文**，1,277 张的卡面节选可校验为完整代码的开头（偏移恒为第 1 行；此数经 ADR-0050 修正，
> 原记 1,262）。
> ADR-0050 独立成篇（代码围栏按宽松候选枚举 + 语法定终点抽取），**订正 ADR-0049 的
> 「那 21 张是真实的代际差异」**：19 张的实现一直在 vault 的 HEAD 里，是选围栏的正则漏读了
> （不锚行首导致配对错位、只认 python 标注看不到 bash 首块），4 张首块是运行方式，
> **只有 2 张是真正的源站独有**。同时修掉一条同类缺陷：卡里的 Python 会把整张 markdown 卡
> 塞进三引号字符串，围栏边界落在串里，6 张已「确证」的卡其实是被截断的
> （最大一张 66 行 → 443 行）。
> ADR-0051 独立成篇（`parses=False` 拆两类），**订正 ADR-0049 收尾那句「余下 20 张是源码
> 本身写坏（全角标点）」**：那 20 张是 **5 张源码写坏 + 15 张判据没定出终点（吞了卡正文）**。
> 分界尺子是「被选中围栏之后还剩几个边界」，20/20 无例外；5 张的缺陷形态各不相同
> （嵌套三引号 / 控制字符 U+0001 / 括号种类不匹配 / 三引号未收尾 / 模块名带连字符），
> 没有一条来自批量标点替换 —— 全角 `（` 出现在 1,110 张卡的恢复区里，其中 1,091 张正常 parse。
> 各插件历史决策（如 AI全栈 ADR-0001~0007、万物互联 D1-D5）保留在各插件 docs/ 内；历史 6 篇 ADR 的归档在三期进行（ADR-0015）。

## 双轨分职（ADR-0015）

| 体系 | 位置 | 职责 | 强制机制 |
| --- | --- | --- | --- |
| ADR | `docs/adr/ADR-NNNN.md` | 编号时间线 + 稳定链接锚：决定**是什么** | 编号连续、索引与文件一致 |
| Note | `docs/notes/{lifecycle}/{class}/yyyy-mm-dd-topic.md` | 为什么改、放弃了什么 | 非机械改动必须同 PR 附一篇 |

两轨以 ADR 头部的 `决策记录` 行与 Note 正文中的 ADR 引用双向互链，由门禁校验可达。

## 模板（ADR-NNNN）

```markdown
# ADR-NNNN · 标题

- 状态：proposed | accepted | rejected | superseded by ADR-XXXX
- 日期：YYYY-MM-DD
- 决策者：<人/角色>
- 决策记录：[Note](../notes/{lifecycle}/{class}/YYYY-MM-DD-topic.md)

## 背景
<为什么需要决策>

## 决策
<我们决定做什么>

## 备选方案
<考虑过的其他方案与取舍>

## 后果
<正面/负面/后续动作>
```
